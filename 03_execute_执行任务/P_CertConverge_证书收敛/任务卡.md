# P_CertConverge — 证书/Keystore 唯一真源收敛

> ⚠️ **2026-06-30 SUPERSEDED**：本任务卡为 v1「官替一把 + 共存一把」目标设计；用户已于 2026-06-30 拍板回合为「**v2 一套配方**」（官替 / 共存 共用 official jks → `e3e13a49`），本任务卡所列各项要么被 v2 路径覆盖、要么需重设计。**新真源 = `docs/RELEASE_LINE_SSOT_发行线统一口径.md` v2 + `DECISION_LOG.md` D-019**。本卡内容仅供溯源，不要按此执行。
>
> 状态变更：⬜ 待领 → ✅ 已被 v2 覆盖（2026-06-30 MMK12，cert-converge-v2）。

---

> 登记：2026-06-29 · Vchat guard_native-FSI67  
> 状态（历史）：⬜ 待领（**无客户窗口期**，宜在本轮收口）  
> 前置只读：`04_review_审稿复核/CERT_RECON_证书对账表_20260629.md`  
> 角色链：总调度登记 → **网络安全官 + 发版官** 共审 → 执行 → 质检 L1

---

## 一句话

把「官替 release = `e3e13a49` / 共存 release = `8f47a47a` / debug = `ca421ec3` 仅开发签」收成**唯一真源 + 硬闸**，消灭 D-017「同 keystore」与旧文档 `ca421ec3` 唯一口径；**无客户，现在改成本最低**。

---

## 背景（L1 已证）

- 共存 debug 装机冷启：`[cp] cert mismatch` → 蜜罐影子期 → A2 散沙（pid 17808，2026-06-29）。
- 根因：实际签 `ca421ec3` ≠ build.gradle coexist EXPECTED `8f47a47a` ≠ registry 仍绑 official `e3e13a49`。
- cert-sync-v1（P_CertSync ✅）已在 **build.gradle + cert-sync 任务书**写 per-flavor；**DECISION_LOG D-017 / 发版 skill / 共存差异清单**仍写「同一把」→ 文档互打架。

---

## 目标（Done 定义）

1. **决策真源**：DECISION_LOG 新增条目（或 D-017 标 DEPRECATED），明文：**官替 / 共存各一把 release keystore，debug 仅开发签，不得混用做 L1 发版门控**。
2. **代码接线**：CMake 按 flavor 选 `registry_cipher.inc` vs `registry_cipher_coexist.inc`（G2 缺口）。
3. **硬闸**：`tools/verify_cert_chain.ps1`（或 gradle task）— 四端（apksigner / BuildConfig / registry inc / 预期 certBind）逐字节比，FAIL → BLOCK assemble。
4. **L1 清单**：RELEASE_RULES D 步 + 发版 skill 收尾 + PROTECTION_MAP §9 补 3 条：`[cp] cert mismatch`=0 · certBind 前缀=EXPECTED · release 变体四端 ✅。
5. **文档减法**：按 CERT_RECON §3.A 批量改/标 DEPRECATED（发版 skill、PROTECTION_MAP §10.9、RELEASE_RULES 快查分 dev/release 两章）；**不新增平行 SOP**。
6. **debug 政策拍板**（架构师选一，写进 DECISION_LOG）：
   - **A** debug EXPECTED+registry 也绑 ca421ec3（日常 assemble*Debug 不误伤）
   - **B** debug 也签 release key（四端天然对齐，碰正式 key）
   - **C** 接受 debug mismatch，**禁止 debug 做发版 L1**（最小改码，须硬闸 enforce）

---

## 不在范围

- A2 签名轴 hook 逻辑改动
- miyou-server 部署（除非 coexist release_id 接线需要加线）
- 删 Filter fallback（归 P_RegistryUnify）

---

## 验证（必须 L1 落盘）

| 变体 | 必过 |
|------|------|
| officialRelease | 四端=e3e13a49；无 `[cp] cert mismatch`；`tamper=正常` |
| coexistRelease | 四端=8f47a47a；registry 解得开；无 cert mismatch |
| *Debug | 按 §6 选定政策验收（A/C 至少不误进影子期或明确标注 dev-only） |

命令见 `CERT_RECON_证书对账表_20260629.md` §7。

---

## 回滚点

- git 快照（改 build.gradle / CMake / kdf / inc 前）
- 保留 `registry_cipher.inc` 与 coexist inc 生成前 fingerprint
