# 真锁线代码核查 — 20260629（只读侦察 · G8 代码即真相）

> 角色：真锁代码核查官（只读侦察）+ 架构师复核（本会话 Vchat `guard_native-MJ25`）。
> 全程零改代码/配置；本文为指定产出。L1=动态(logcat/Frida) ｜ L2=静态读码 ｜ L4=待验。
> 关系：取代 `R3` / `R5_加密进度核查_20260625.md` / `STATUS_防封加密线.md:63` / 两份 SSOT 中关于 ①②③ / G2 / G4 / coexist registry 的**过时**结论（cert-sync / registry-unify 提交后已不成立）。机制真源不变：`授权风险场景矩阵_SPEC.md` / `DESIGN.md` / 安全官 skill。
> ⚠️ **pre-D-026 快照**：本文成于 2026-06-29 cert-sync v1（两把印章）期，文中「共存 cert `8f47a47a` / `registry_cipher_coexist.inc`」（见 §2 :61 / §3 :69）为当时现码真相；**2026-06-30 D-026 已把共存并入 `e3e13a49` 一套配方、`coexist.inc` 退役**（现状真源 = `DECISION_LOG.md` D-026 / `docs/RELEASE_LINE_SSOT_发行线统一口径.md`）。

---

## 0. 一句话

代码已整体推进到 **D-020/D-021 + 今日 cert-sync/registry-unify**：两闸独立成立、真锁链完整落码（L2）。**最大短板没变 = 全线缺 D-020 的 L1**（磁盘唯一 A2 logcat 是旧 D-018）。**G2/G4/coexist registry 今天被大幅推进，旧文档结论过时**（本次重点）。

⚠️ G8 事实：`.devin` 那个「拆 A2 闸到 `AntiBanGate.java`」**没落地** —— `AntiBanGate.java` 不存在，A2 闸现仍在 `GuardRuntime.java`（防后续 AI 照 .devin 任务误判已拆）。

---

## 1. 逐项表

### 组1 两闸

| 闸 | 代码 file:line | L |
|---|---|---|
| A2 `isAntiBanReady` | `GuardRuntime.java:138-148`（先 cert 拦截→时间闸）+ 纯函数 `evalAntiBanWindow:176-189`（72h/7d 全分支）+ 常量 `:135-136` + 8 分支自测 `:221-238` | L2（L1✗ 仅旧 D-018） |
| 隐私 `isActive` 四层 | `StateMachine.java:93-98`（`isVipAuthorized && isSensitiveConfigReady && f1 && mActive`）；第 2 层 = 真防护根因 | L2 |

### 组2 场景矩阵 11 项 → 代码锚点（与 SPEC 一致，无重大漂移）

- #1 首装 72h：`evalAntiBanWindow officialBase+72h :187-188` ✅
- #4 到期：隐私立刻关 `isAuthorizedNow license :167` / A2 7 天 `:185` ✅
- #5#6 封停删卡：隐私 rf 立刻散 `isAuthorizedNow:157`、A2 72h `cardRevokedAt+72h :181` ✅
- #7 篡改：`CompatProbe.isIntegrityIntact`（`GuardRuntime:140`，canary 刻意不进门）→影子期 ✅
- #10/#11 root：代码无 root 扫描 = 符合「不硬查」✅
- 唯一债：#3 续费弹窗走独立 AlertDialog 未走 `RiskPromptController`（红线#9，SPEC §4 自认）

### 组3 真锁链（全 L2，server seed 有 L1）

| 环 | file:line |
|---|---|
| AES-GCM | `config_crypto.cpp:396-417` decrypt + `:283-335` gcm_decrypt |
| cert 绑定 | `:346 g_binding` + `:419 set_binding_material` + `derive_registry_key fold :519-523` |
| server seed | `:352 g_server_seed` + `unwrap_server_seed:456-495` + `registry_loader.cpp:201`（无 seed→scatter）；L1：有种子 recipeOk=true（R5） |
| Ed25519 | `AuthEnvelopeVerifier`（每次重验，R5:152-168） |
| KDF 三端 | `derive_registry/bootstrap/wrap` + `kdf_self_test:615`；三端 GREEN 06-27（STATUS） |
| per-flavor registry | `registry_loader.cpp:33-37` + `CMakeLists.txt:42` |

### 组5 钥匙加固

| 项 | file:line | 状态 |
|---|---|---|
| 全局 W | `config_crypto.cpp:374-375 g_wk_lo/g_wk_hi` | 🔴 仍在（删=Batch3 待 dm≥90%） |
| W_dev 双试 | `unwrap_server_seed:474-494`（①W_dev②全局W）+ `derive_wrap_key:577` | ✅ L2 |
| 牙④ 过期 | `seed_expired:389-392` + `unwrap 检查 :469` + `set_envelope_expiry:447` + `envelope_expiry_self_test:692` | ✅ L2（仅编译期自测，无 release L1） |

---

## 2. 漂移清单（旧文档说 X，现码实为 Y）★ 重点

| # | 旧文档 | 现码真相 | 判 |
|---|---|---|---|
| 1 | STATUS:63 / R5 §④G4「release_id 硬编码 android_8071 未 flavor 注入」 | `build.gradle:138/156` per-flavor + `AppConfig.java:52` 读 `BuildConfig.GUARD_RELEASE_ID` | **过时** |
| 2 | R3 / R5 §④G2「CompatProbe.EXPECTED_CERT 硬编码 ca421ec3」 | `CompatProbe.java:45` 读 `BuildConfig.GUARD_EXPECTED_CERT`；`build.gradle:136`官替 e3e13a49 / `:151`共存 8f47a47a | **过时** |
| 3 | CERT_RECON / 发行线 SSOT §3 D3「coexist.inc 未接 CMake」 | `CMakeLists.txt:42` 按包名定义 `GUARD_REGISTRY_COEXIST` + `registry_loader.cpp:33` 二选一 | **过时**（cert-sync 后已接） |
| 4 | A2 SSOT §9 / §3.2 / §7 / §8：①②③「未落码 / 未接 / 永久不自愈」 | ①时间闸 ②官方对时 ③latch 可恢复 均已落码（见组1/2） | **过时**（待回改） |

---

## 3. 架构师补的两条 L1（本会话 MJ25 实测，报告够不着）

- **coexist registry cert 绑定 = L1 确认正确**（原报告/SSOT 标 L4 待验，可升级）：实测 AES-GCM 解密 —— `registry_cipher_coexist.inc` 用 `8f47a47a` 派生 key **能解开**、用 `e3e13a49` **解不开（InvalidTag）**；`registry_cipher.inc` 反之。共存配方绑对了。KDF 用 `kdf_common`（= SO/gen 同一套，三端镜像 GREEN），故 SO 运行时同样可解。
- **官替 `registry_cipher.inc` 本会话已重生成**：原 inc（1:38 生成）落后于 registry 源（2:11 registry-unify），缺 `moments.feed` 4 锚点（`ii5.b`/`jw1.d`/`wq.c1`/`wq.y0`）；用 `gen_registry_cipher.py --recipe android_8071.json` 脚本重生成，pt 2555→2640 == 当前源（L1 解密验，cert 仍 e3e13a49）。**未提交**（后续还要改，按用户口径暂放）。

---

## 4. 真缺口 + 下一步

1. 🔴 **全线缺 D-020 的 L1**：出新包装机抓 `[ANTIBAN-BRANCH] *_over72h PASS` / `official base set` / `card-revoked cleared` 三类 logcat（代码 OK，缺设备证据）。
2. 🟡 **coexist registry 装机 L1**：cert 绑定已 L1（§3）；仍缺 coexistRelease 包 + 空机装机 `recipeOk=true`（G5）。
3. 🔴 **全局 W 删除（Batch3）**：等服务器 dm 回填 ≥90% + 观察窗。
4. 🟡 **C7 接 6 剩项**：`ContactLabelHideGuard.java:31 fc5.g` 仍内联；`PushFilter` 仍零 registry（C6）。MomentsFilter 今日已归一。
5. 🟡 **续费弹窗红线#9 债**：`SettingsEntry.showRenewReminderIfNeeded` 走独立 AlertDialog，块D 收口统一到 `RiskPromptController`。

*核查完毕。无代码改动。本文为指定产出，取代旧快照过时结论（见头部）。*
