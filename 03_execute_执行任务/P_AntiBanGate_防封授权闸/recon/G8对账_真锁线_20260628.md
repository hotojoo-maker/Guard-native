# G8 对账 — 真锁线「代码真相 vs SSOT §9 待办表」（只读侦察）

> 角色：只读侦察（只读、不改代码、结论标 L 级）。窗序号 FSI34。日期 2026-06-28。
> 真相基准：G8「代码即真相」——文档与代码冲突以代码为准。
> 对账对象：`SSOT_A2授权防破解_统一真源.md` §8 锚点表 / §9 待办表。
> 方法：逐项读源文件取 file:line 证据 + 标 L 级 + 查 logs/ + recon/ 有无 L1 装机日志。
> L 级口径：L1=动态(logcat/Frida)已证；L2=静态(代码/反编译)已证；L4=待验证。

---

## 0. 一句话总结

**代码已整体推进到 D-020，但 SSOT §9 待办表严重滞后（G8 漂移）**：①②③ 已落码（L2），SSOT §9 仍写「未落码/未接/永久不自愈」；④⑤⑥ 与 SSOT 一致、无漂移。**全线缺 D-020 的 L1**：磁盘上唯一 A2 logcat（`refund_e2e_*`，2026-06-27 02:25）是**上一版 D-018**（cert-only 闸 `gate=local-cert` + 旧 `refunded` latch），**无任何 D-020 时间闸 / 官方对时 / card-revoked 标记** → ①②③ 代码从未在设备跑过。

---

## 1. 逐项对账

### ① A2 时间闸（`GuardRuntime.isAntiBanReady` + `evalAntiBanWindow` 72h/7天）
- **现状**：✅ 已落码。`isAntiBanReady`（先 cert 拦截→再时间闸）→ `isWithinAntiBanWindow` → 纯函数 `evalAntiBanWindow`（授权中装 / 封停 `cardRevokedAt+72h` / 曾授权到期 `licenseExpire+7天` / 首装 `officialBase+72h` / 各无值 fail-open）。另有 DEBUG 自测 `antiBanGateSelfTest` + `antiBanBranchSelfTest`（8 分支 expect/actual）。
- **证据**：`core/GuardRuntime.java:138-189`（闸 + 纯函数全分支）、`:135-136`（72h/7天常量）、`:221-238`（分支自测）。
- **L 级**：L2。
- **L1**：✗ 无。`logs/refund_e2e_logcat.txt:2074` 仅 `[ANTIBAN-GATE] ready=true ... gate=local-cert; D-018`，无 `ANTIBAN-BRANCH` / `time-gate` / `D-020` 行。
- **vs SSOT**：§9「A2 时间闸（72h/7天）— 未落码」= **过时**（G8 漂移）。§3.2 `[码]`「无时间闸」亦过时。
- **真没做的下一步**：出新包装机，抓 `[ANTIBAN-BRANCH] *_over72h ... PASS` + `[ANTIBAN-GATE] ...(D-020: cert + time-gate 72h/7d)` 落盘，并改 SSOT §9/§3.2。

### ② 官方对时第二源（`OfficialClock.readOfficialNowMs(hd.b)` + ModuleMain §6.55 + `LeaseClock.noteOfficialTime`）
- **现状**：✅ 已落码 + 已接线。`OfficialClock.readOfficialNowMs(jy0.hd.b())` 纯读反射；`LeaseClock.noteOfficialTime`（只抬不降 + 2年未来上限→返回 true 让调用方 `markTampered`）+ `getOfficialBaseMs`（72h 起算锚）。ModuleMain §6.55 冷启动读→喂→异常未来值 `markTampered`，**置于 A2 闸（§6.6/§6.7）之前**。
- **证据**：`core/OfficialClock.java:36-51`、`core/LeaseClock.java:85-111`（noteOfficialTime/getOfficialBaseMs）、`ModuleMain.java:217-229`（§6.55 接线）。
- **L 级**：锚点 `hd.b` L1 已证；接线代码 L2。
- **L1**：锚点 ✓ L1（`recon/convtime_L1_evidence_20260627.log:29-37`：改表 +1h，`hd.b` 纹丝不动、`hd.c` 跟墙钟 → hd.b 抗改表，决定性）。接线本体 ✗（logcat 无 `[oclk]` / `official base set`）。
- **vs SSOT**：§7『`noteOfficialTime` 未接』、§8『未接』、§9『未接』= **全部过时**（G8 漂移）。
- **真没做的下一步**：装机抓 `[lease] official base set (72h countdown anchor)` + `[oclk]` 行；改 SSOT §7/§8/§9。

### ③ latch 可恢复（`EnvelopeStore.clearCardRevokedIfAuthorized`）
- **现状**：✅ 已落码。`saveEnvelope` 按信封 `rf` 位分流：`cardRevoked==1`→`markCardRevoked()`（幂等记首见）；否则→`clearCardRevokedIfAuthorized(e)`（rf≠1 + 验签 + license 未过期 → 清 `rf` latch、两闸恢复；本地改时间/清缓存不触发，须 Ed25519 验签）。
- **证据**：`net/EnvelopeStore.java:233-240`（clearCardRevokedIfAuthorized）、`:113-117`（saveEnvelope 分流）、`:205-211`（markCardRevoked 幂等）。
- **L 级**：L2。
- **L1**：✗ 新可恢复路径无 L1。旧 D-018 `refunded=true` **立刻撤** 有 L1（`logs/refund_e2e_blockB.txt:1472` `[ANTIBAN-GATE] ready=false ... refunded=true`，但属旧命名、旧立刻撤逻辑、非 D-020 72h+恢复）。
- **vs SSOT**：§3.4『现有 latch 永久不自愈要改』、§8『永久不自愈（待改）』、§9『latch 改可恢复 — 现永久不自愈』= **过时**（代码已可恢复，G8 漂移）。
- **真没做的下一步**：端到端装机验「封停撤→发新有效信封→恢复」，抓 `[env] card-revoked cleared by valid envelope → restore A2+privacy`；改 SSOT §3.4/§8/§9。

### ④ 牙④ 过期绑定（`pushSeedExpiryToNative` + `NativeBridge.setEnvelopeExpiry` + SO `seed_expired`）
- **现状**：✅ 已落码（全链）。Java `pushSeedExpiryToNative`（hard_expire=leaseExpire+7天宽限，leaseExpire≤0→推 0 关检查）→ `NativeBridge.setEnvelopeExpiry`→native `set_envelope_expiry` 存 `g_seed_hard_expire/g_seed_trusted_now`；`unwrap_server_seed` 成功路径**前**调 `seed_expired()`（`trusted_now>=hard_expire` 且二者均>0）→ 散沙、**不折静态 key**。另有 C++ `envelope_expiry_self_test`。
- **证据**：`net/EnvelopeStore.java:280-285`、`core/NativeBridge.java:368-371,409`、`native_core/src/config_crypto.cpp:355-360,387-392,447-450,469,692-705`。
- **L 级**：L2。
- **L1**：✗（无捕获 PASS；`envelope_expiry_self_test` 为 `GUARD_DEV_SELFTEST` 编译期、非 release）。
- **vs SSOT**：§5 牙④『a 案已落码』、§8 牙④、安全官 skill 阶段计划#10 = **一致 ✓ 无漂移**。
- **真没做的下一步**：DEBUG 编译跑 `envelope_expiry_self_test` 抓 PASS 落盘，或端到端用过期信封装机验 `recipeOk=false` 散沙。

### ⑤ Batch 全局 W（`config_crypto.cpp` 全局 `g_wk` 删没 / `W_dev` 双试）
- **现状**：🔴 全局 W **仍在**（`g_wk_lo`/`g_wk_hi` 16B 明文常量）。`W_dev` 双试**已落**（`derive_wrap_key` + `unwrap_server_seed` ①试 W_dev ②回退全局 W）。Batch3（删全局 W）**未做**。
- **证据**：`native_core/src/config_crypto.cpp:374-375`（全局 W 明文）、`:473-489`（①W_dev②global W 双试）、`:577`（derive_wrap_key）、`:658-663`（KDF 向量自测）。
- **L 级**：L2。
- **L1**：不在本次范围（W_dev 灰度归服务器线，见 `I:\miyou-server\REVIEW_2026-06-27.md`）。
- **vs SSOT**：§5『Batch3 删全局 W 未做』、§9、安全官 skill 阶段计划#9 = **一致 ✓ 无漂移**。
- **真没做的下一步**：等服务器 `dm` 回填 ≥90% + 观察窗 → 删 `g_wk_lo/g_wk_hi` + 备迁移方案（缺则全量正版散沙）。

### ⑥ canary② 绑弹窗行为（`CompatProbe` 现状）
- **现状**：🟡 `CompatProbe` 存在，canary = **诱饵指纹** `BASELINE`（`fp(PROMO_URL)^fp(PROMO_TOKEN)^enabled`）+ **cert** 绊线（`checkSignature` / A2 门 `isIntegrityIntact`）。canary②「hash 弹窗稳定段 / 绑行为」**未做** → 留诱饵常量、只 NOP 弹窗显示 = 不触发 canary（但重签必触发 cert 兜底）。
- **证据**：`core/CompatProbe.java:30`（BASELINE）、`:42-54`（check 诱饵指纹）、`:71-90`（checkSignature）、`:105-127`（isIntegrityIntact A2 门）。
- **L 级**：L2。
- **L1**：cert 绊线/A2 门 L1 已证（`logs/refund_e2e_logcat.txt:2074` `certIntegrityIntact=true`）；canary② 本就未做。
- **vs SSOT**：§4 诚实缺口『canary② 未做』、§9 = **一致 ✓ 无漂移**。
- **真没做的下一步**：设计弹窗稳定段 hash → 进 `CompatProbe.check` 走 COMPARE（不折 key、走影子期），堵「只 NOP 弹窗显示」绕过。

---

## 2. 汇总表

| # | 项 | 代码现状 | L级 | L1 装机 | vs SSOT §9 |
|---|---|---|:--:|:--:|---|
| ① | A2 时间闸 72h/7天 | ✅ 已落码 | L2 | ✗（仅旧 D-018 cert-only） | 🔴 过时（写「未落码」） |
| ② | 官方对时第二源接线 | ✅ 已落码+接线 | L2（锚点 L1） | ✗（锚点 ✓） | 🔴 过时（写「未接」） |
| ③ | latch 可恢复 | ✅ 已落码 | L2 | ✗（仅旧 refunded 立刻撤） | 🔴 过时（写「永久不自愈」） |
| ④ | 牙④ 过期绑定 | ✅ 已落码 | L2 | ✗ | 🟢 一致 |
| ⑤ | Batch3 删全局 W | 🔴 全局W仍在/W_dev双试已落 | L2 | n/a | 🟢 一致 |
| ⑥ | canary② 绑行为 | 🟡 仅诱饵指纹+cert | L2 | cert✓ / canary②未做 | 🟢 一致 |

---

## 3. 给接手 AI 的两条提醒（不改文档，仅建议）

1. **SSOT §3.2 / §7 / §8 / §9 关于 ①②③ 的「未做」描述与代码冲突，按 G8 应以代码为准**——但本侦察只读不改 md（G5）。是否回改 SSOT 由用户拍板（建议把 ①②③ 从「待办」移到「已落码·待 L1」）。
2. **①②③ 的真缺口是 L1，不是代码**：需出 D-020 新包装机，抓 `ANTIBAN-BRANCH PASS` / `official base set` / `card-revoked cleared` 三类 logcat 落盘后，才能从 L2 升 L1。现有 `refund_e2e_*` logcat 是上一版 D-018，不可当 D-020 证据。

*侦察完毕。无代码改动、无既有 md 改动；本文件为任务指定产出。*
