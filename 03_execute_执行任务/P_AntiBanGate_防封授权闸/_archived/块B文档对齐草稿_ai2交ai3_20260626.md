# 块B 文档对齐草稿（文档资料ai-2 → 架构师-ai-3）

> **性质**：草稿/清单，**不是已落定稿**。根目录共享文档（STATUS/DECISION_LOG/SPEC）由架构师-ai-3 落；P 任务文档（DESIGN/红队）是否由我 in-place 改，**等 ai-3 拍**（见 §0 问题）。
> 日期：2026-06-26 · Vchat `文档资料ai-2`（质检/文档，纯文档、零碰码）。
> 证据口径：L2 = 读码确认（本稿锚点均 L2）；L1 = 装机原文（块B 4 条**待验**，未跑 → **不标 L1**，G6）。
> 已核现码（G8）：`GuardRuntime` 135-140 · `RiskState` 92-152 · `EnvelopeStore` 47/146-214 · `worklog.md` 2026-06-26e。

---

## 0. ⚠️ 并发警报 + 一个要 ai-3 拍的问题

**并发实况**：上一棒（同会话 `文档资料ai-1`，架构师批的「全做 A」）我已**直接落**了一批 P 任务/skill 文档编辑。**与此同时**，另一窗口（`本地开发ai-1·F22`）把**块B 代码落了**（worklog `2026-06-26e`，8 文件，L2·装机待跑）。结果：我上一棒的 **3 处编辑因块B落码而 stale**（见 §1）。

**问题（请 ai-3 拍）**：这 3 处 stale 在 **DESIGN.md / 红队稿**（P 任务文档，非根目录共享文档）。要我：
- **(A)** 直接 in-place 改这 3 处（我上一棒就是直接改的，P 任务文档不在「根目录共享」红线内）？还是
- **(B)** 我只交本草稿、由 ai-3 统一落？

我已在本草稿 §1 给出**逐字修正文**，A/B 皆可用。

---

## 1. 三处 stale（我上一棒写的，现因块B落码而过时）+ 逐字修正

> 共同根因：块B 把 `isAntiBanReady` 改成 `!EnvelopeStore.isRefunded() && CompatProbe.isIntegrityIntact`（`GuardRuntime` 138-139），**退款撤A2 已落码（L2 装机待跑）**；我上一棒写的「A2 侧未接 / L4」全部作废。

### Stale-1 · `DESIGN.md §6` 退款脚注 + 伪码体

**现（我上一棒写的，stale）**：伪码体只 `return CompatProbe.isIntegrityIntact(...)`；脚注写「A2 侧 !isRefunded 尚未接进 GuardRuntime.isAntiBanReady —— L4 待落码」。

**修正（对齐现码）**：
```
isAntiBanReady(ctx, modulePath):
  if EnvelopeStore.isRefunded(): return false   # 退款=唯一连坐A2的非篡改场景(SPEC §4)，已落码(块B)
  return CompatProbe.isIntegrityIntact(ctx, modulePath)
```
脚注改为：
> 退款（SPEC §4）已落码（块B 2026-06-26e，L2 装机待跑）：`isAntiBanReady = !isRefunded && isIntegrityIntact`；隐私侧 `isRefunded→isAuthorizedNow`、`markRefunded`/`debugSetRefunded`/`/api/forcerefund` 均已落。**仅信号源 server push（rf 位下发）留块A 暂缓**——live 生产退款触发待块A，本地撞闸 + debug 验已具备。

### Stale-2 · 红队稿 `T15` 现状行

**现（stale）**：「A2 侧未落码……未 AND `!isRefunded`（L4 依块B）；故 live 退款撤 A2 当前不生效」。

**修正**：
> | 现状(L2·装机待跑) | 客户端撞闸**已落码**（块B 2026-06-26e）：`isAntiBanReady = !isRefunded && isIntegrityIntact`（撤A2）+ `isAuthorizedNow` 撤隐私 + `markRefunded`/`debugSetRefunded`/`/api/forcerefund`。**仅 server push（rf 下发）留块A 暂缓**。装机验：`/api/forcerefund {on:true}` → 冷启/回前台 → `[ANTIBAN-GATE] ready=false refunded=true`（A2 不装）+ 密友重现（isActive 断）+ 0 崩溃（worklog 待验#1）|

锚点行补：`GuardRuntime.isAntiBanReady` 135-140；`DebugServer./api/forcerefund` 143/473。

### Stale-3 · 红队稿 `§4` 缺口表 #4 行

**现（stale）**：「🟡 退款撤 A2 未接（#6）…… A2 侧 isAntiBanReady 未 AND !isRefunded（L4 依块B）」。

**修正**：
> | 4 | 🟡 **退款 server push 信号源未建（#6）** | 客户端撞闸**已落码**（`isAntiBanReady=!isRefunded&&isIntegrityIntact` + `isAuthorizedNow` 撤隐私 + `/api/forcerefund`，L2 装机待跑）；**仅 server push（rf 下发）留块A 暂缓** | live 生产退款触发待块A；本地 + debug 已可验；见 T15 |

**另：§4 #2 时间闸行**小修——「T_soft 软引流」由「OFFLINE_FUNNEL+续费提醒已覆盖大部」更新为「**已落码**（`RiskState` T_soft=1h + `isPastSoftGrace`→抬 `OFFLINE_FUNNEL` 软弹窗，L2 装机待跑）；T_login 降 v2」。

---

## 2. Task 1 · 红队任务书 #6 / #2 / #10 用例（草稿）

> 现状：#2/#10 我上一棒已直接写进红队稿 **T13/T14**（不受块B影响，**仍准确**，无需改）；#6 的 **T15** 见 §1 Stale-2 修正版。下面给三者**最终应有内容**便于 ai-3 一处核。

- **T13 · #2 付费正版全绿（正向）**：有效卡+授权期内 → `ANTIBAN-GATE ready=true`、`isActive=true`(四层)、`level=正常`、密友隐藏、无 funnel。破防判定=正版却 recipeOk=false/散沙/误隐藏失效。锚点 `GuardRuntime.isAntiBanReady`135、`EnvelopeStore.isAuthorizedNow`146、`StateMachine.isActive`。证据 L1 旁证 `batch1_netverify`。**【已在稿】**
- **T14 · #10 root+付费认正版（正向）**：root+有效卡 → 同 T13 全绿；客户端**不读 root**（铁律5），net 包零 root（T12）。破防判定=root 触发本地散沙/撤A2=误杀。**【已在稿】**
- **T15 · #6 退款立刻散沙**：`/api/forcerefund {on:true}` 或 server push `rf=1` → `markRefunded` → 隐私 `isAuthorizedNow=false` + A2 `isAntiBanReady=false`，不自愈、不清数据。现状见 §1 Stale-2（**已落码 L2 装机待跑；仅 server push 块A**）。**【需按 Stale-2 修正】**

---

## 3. Task 2 · DESIGN §5.1/§6 被 D-018 取代的旧口径清单（标「演进记录·勿照旧实现」）

> 现状：我上一棒已直接重写 §5.1/§6 + 在 §0/§1.1/§3/附录A 加「⚠️D-018取代,详见§6」指针。本清单 = **被取代旧口径逐条登记**，供 ai-3 核「是否每处都已标演进记录」。§6 退款脚注另见 §1 Stale-1。

| 旧口径（被取代） | 出处 | 现口径（D-018 / SPEC §2·§4） |
|---|---|---|
| A2 official DER 锁进 server-seed `registry_pack`、fail-closed、盗版无种子→散沙 | §5.1 旧文 | DER 本地常量（公开值）、**fail-open**；隐私 registry 仍 server-seed fail-closed（划清边界）|
| `isAntiBanReady` 吊 `EnvelopeStore.isAuthorizedNow`+续费/断网宽限+影子租约（白嫖到期撤A2） | §6 旧伪码 | = `!isRefunded && isIntegrityIntact`（本地完整性轴 fail-open，未授权永不撤、退款唯一例外）|
| 红线#1「isAntiBanReady 不是客户端布尔→吊 EnvelopeStore+LeaseClock」 | §6 红线 | 对 A2 闸已不适用（现=本地 cert 轴）；仍适用于隐私闸 |
| 两闸表「isAntiBanReady 吊 EnvelopeStore+LeaseClock+影子租约」 | §0 摘要 | 吊**本地模块证书完整性**（`CompatProbe.isIntegrityIntact`）|
| 总映射表 isAntiBanReady(A2) 列按授权/影子租约分档 | §1.1 | 除「重签→散沙」「退款→撤」外一律装（首装/到期/断网/白嫖保号）|
| 触发线 B「未授权→影子期→卸 A2」解体语义 | §3 | 未授权永不撤 A2；仅 T_soft 软引流 |
| 决策台账 #1/#2/#3/#28（A2 进时间闸/影子租约/到期宽限撤 A2）| 附录A | 时间闸/影子期仅作用于隐私/引流层，不门控 A2 |

**全部标注建议**：上述每处加「⚠️ 演进记录·勿照旧实现，权威见 §6 + DECISION_LOG D-018 + SPEC §3/§4」（我上一棒已加指针，ai-3 核一遍即可）。

---

## 4. Task 3 · SPEC §6 缺口表 · 块B ①②③④ 落码状态清单（待块B装机后由 ai-3 标）

> 块B 代码 worklog `2026-06-26e` 落 8 文件（L2·ReadLints 全零错），**装机 4 条待验**（设备 609b4b18，绿后补 L1）。**未跑 → 不标 L1（G6）**。

| 块B 项 | 落码状态(L2) | 装机(L1) | 残留/归属 |
|---|---|---|---|
| **① 时间闸 T_soft 软引流** | ✅ 已落 `RiskState.T_SOFT_MS=1h` + `KEY_INSTALL_SEEN` + `isPastSoftGrace()`；`evaluate()`：未授权+!退款+超T_soft → 抬 `OFFLINE_FUNNEL`（软弹窗可关，不撤A2、不连坐隐私四链）| ⏳ 待验#3 | T_login **降 v2**（未落，故意，无 Activity 锚点）|
| **② 散沙扩面（只杂项）** | ✅ 已落 `AntiRecall`/`FakeLocation`/`PushFilter`(主进程6闸) 加 `if(isTamperDegraded())return`；`isTamperDegraded`=仅 `TAMPER_FUNNEL/PERSISTENT`（确认篡改超影子期）| ⏳ 待验#2 | 密友隐藏四链**不碰**（record-only 故意，铁律29）；:push 子集不动（铁律30）；⚠️ `FakeLocation` 额外加 `isRefunded`（退款也关定位）= **超字面，待 ai-3 拍保留**（worklog 6⚠️）|
| **③ 退款立刻散沙信号链** | 🟡 客户端撞闸+字段**已落**（`AuthEnvelopeVerifier.refunded` rf 位 / `EnvelopeStore` K_REFUNDED·isRefunded·markRefunded·debugSetRefunded / `isAuthorizedNow` 撤隐私 / `isAntiBanReady` 撤A2 / `/api/forcerefund`）| ⏳ 待验#1 | **server push（rf 下发）= 块A 暂缓未建**；live 生产触发待块A |
| **④ 续费提醒 + risk_pack** | 续费弹窗=**现状保留**（`SettingsEntry.showRenewReminderIfNeeded` 已实现）；`risk_pack` 服务端下发=**未落**（影子期/冷却硬编码 `SHADOW_HOURS_DEFAULT=168` 等）| — | 续费欠**红线#9 单一弹窗源**债 → 块D 收口；risk_pack → 块A+块B接口 |

**不误伤正版（待验#4）**：授权态 `level=正常`、四链+杂项全常态、无引流、0 崩溃。

---

## 5. 交付小结（给 ai-3）

- §1 三处 stale 修正文已备 → 等 ai-3 拍 §0 的 A/B（in-place by me / ai-3 统一落）。
- §2 红队 #2/#10 已在稿且准确；#6 按 §1 Stale-2 修。
- §3 DESIGN 旧口径清单（演进记录）供核。
- §4 块B ①②③④ 状态清单 → 块B 装机绿后由 ai-3 在 SPEC §6 / STATUS 标 L1。
- 另：根目录待 ai-3 落项（我没碰）——SPEC §5 把《共存差异清单》指过去（文件已建）；STATUS 块C「12 真机用例」→ 现 15（T13-15）；DECISION_LOG D-018 可补「文档对齐 ✅」。
