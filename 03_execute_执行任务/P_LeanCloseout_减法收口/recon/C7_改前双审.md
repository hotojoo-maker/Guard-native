# C7 改前双审（删8 + 接6；缓1 不动）· 只读

> **角色**：Guard Native 执行 AI（只读核账 + 双审，**未改任何代码/json/SO/文档正文**；本文件 = 唯一新建产物）
> **输入**：`R2b_C7挂空对账.md`（权威：删8/接6/缓1）+ R1/R2 佐证 + 网络安全官 skill + 授权检查官 skill + 任务卡 §4/§5/§6/§8
> **日期**：2026-06-25 · **红线**：只读 · 标级 L1~L4 · G1 不猜 · 无 L1/L2 证据不下结论
> **总结论先放**：**WARN**（不是纯 json 删——删8 里有 3 个被 SO 自检 `field_eq` 钉死，删 json 必须同步改 `registry_loader.cpp` + 重编 SO，否则 `registry_self_test()` 假 → scatter/verify FAIL，F-31 式静默全挂）。

---

## 0. 一句话（L2）

- **接6** = 干净（install 期取值、值逐字节一致、SO 自检不碰）→ 可直接接，照 C5a 同法。
- **删8** = **拆成 删5（纯 json）+ 删3（json + 同步删 SO 自检 field_eq + 重编 SO + 装机回归）**。
- **缓1**（`conv.list/item_class`=kc5.y）= 在回调体内，铁律29，本轮不动（已确认）。
- 影响面：动 `registry_8071.json` → 必 regen `registry_cipher.inc`（`gen_registry_cipher.py --recipe android_8071`）+ `RegistryFallback.java`（`gen_registry_fallback.py`）；**KDF/s_rel 不变** → 测试卡 `DEVTEST-3AB7ED` 仍解得开。

---

## 1. 删8 核账（grep 实证 + SO 自检影响）

### 1.1 全仓「真读 key」集合（L1 grep · `src/main/java`）

`recipe()/recipeArr()/getRecipe` 实读 key 仅 = **22 个 C5a 字段** + 负向探针（`no.such.gateway`/`no_such_field`/`__no_such_key__`）。读取点：ConvFilter:119-121 · MomentsFilter:95-102 · ContactFilter:79-84 · SearchFilter:97-101 · GuardHeartbeat:111-112 · ModuleMain:415-416 · DebugServer:184-185。**删8 的 8 个 key 全部不在此集合**（逐项见下）。

### 1.2 逐字段裁决

| # | entry / 字段（值） | Filter 读？ | SO 自检 `field_eq`？ | 删法 | 级 |
|:--:|---|:--:|:--:|---|:--:|
| 1 | conv.list / **l4_notify** (notifyDataSetChanged) | 否 | **是 · registry_loader.cpp:314** | **json + 删 SO 自检:314 + 重编** | L1 |
| 2 | conv.list / **contact_class** (…storage.l4) | 否（仅 contact.address/contact_class 被读=ContactFilter:83，**异 entry**） | 否 | 纯 json | L2（删前 L4 复核反射全名加载） |
| 3 | moments.feed / **list_addall** (ArrayList/LinkedList.addAll) | 否 | 否 | 纯 json | L1 |
| 4 | moments.feed / **actor_class** (z15.e56) | 否 | 否 | 纯 json | L2 |
| 5 | moments.feed / **actor_wxid_field** (f435583d) | 否 | **是 · registry_loader.cpp:324** | **json + 删 SO 自检:324 + 重编** | L1 |
| 6 | contact.address / **list_addall** (ArrayList.addAll) | 否 | 否 | 纯 json | L1 |
| 7 | search.gateway / **policy** (hide_if_target_in_hidden_union) | 否（DebugServer:307 读 HTTP body 同名参，非 registry） | 否 | 纯 json | L1 |
| 8 | search.gateway / **unlock_entry** (excluded_search_unlock_111111) | 否 | **是 · registry_loader.cpp:338** | **json + 删 SO 自检:338 + 重编** | L1 |

### 1.3 ★ 关键发现：3 个删字段是 SO 自检 canary（R1/R2b §5.2「未核 *.cpp」补全）

`native_core/src/registry_loader.cpp::registry_self_test()`（L301）用 `field_eq` 硬断言：
- `:314` conv.list/l4_notify == "notifyDataSetChanged"
- `:324` moments.feed/actor_wxid_field == "f435583d"
- `:338` search.gateway/unlock_entry == "excluded_search_unlock_111111"

→ 这 3 个虽无 Filter 读，但充当**「registry 解密出来值对不对」的完整性 canary**。**删 json 不删这 3 行断言 → `registry_self_test()` 返回 false → 出包体检 FAIL（PHASE/BATCH 链），等同 F-31 静默挂**。
- `registry_dump_summary()`（:293-295）也读 `l4_notify` 作展示，但**容错**（缺则不显示 `l4=`，不报错）——非阻塞。
- `entries==4`（:308）= **顶层 entry 计数**；删8 删的是**子字段**，4 个顶层 entry（conv.list/moments.feed/contact.address/search.gateway）保留 → **entries==4 不破** ✅。

### 1.4 删8 落地建议（拆两批）

- **删5（纯 json，低风险）**：conv.list/contact_class · moments.feed/list_addall · moments.feed/actor_class · contact.address/list_addall · search.gateway/policy。
- **删3（json + SO 自检同步，需重编 + 装机回归）**：conv.list/l4_notify(:314) · moments.feed/actor_wxid_field(:324) · search.gateway/unlock_entry(:338)。
  - 安全官口径：self-test = **出包前体检**，删「死字段断言」**不削弱真锁**（真锁 = 服务器种子 + KDF，不在这 3 个 field_eq）；但**必须 json 与 .cpp 同源同删**，否则体检红。

---

## 2. 接6 核账（file:line + 非回调体 + 值逐字节一致）

| # | entry / 字段（registry 值） | 代码内联副本 file:line | 接入点（resolveRecipes 加 recipe） | 在回调体内？ | 值逐字节一致 | 级 |
|:--:|---|---|---|:--:|:--:|:--:|
| 1 | conv.list / **l1_methods** (n,m) | install 扫描内联 `"n"/"m"` ConvFilter:391（+ L3 排除:493） | resolveRecipes + installMvvmListHooks/L3 | **否**（install 期 `installMvvmListHooks`，非 hook 回调体） | 是（"n","m"） | L2 |
| 2 | conv.list / **l2_method** (s) | install 扫描内联 `"s"` ConvFilter:447（+ L3 排除:493） | 同上 | **否**（install 期） | 是（"s"） | L2 |
| 3 | conv.list / **contact_fields** (d,e,f,a,b,c) | 字段 `CONTACT_FIELD_NAMES` ConvFilter:72 | resolveRecipes `recipeArr("contact_fields")` | 否（字段初值，消费在 :1981 等） | 是 | L2 |
| 4 | moments.feed / **actor_field_names** (d,f435583d,username,field_userName) | 字段 `ACTOR_FIELD_NAMES` MomentsFilter:72-74，消费 :632/:1232/:1242 | resolveRecipes `recipeArr("actor_field_names")` | 否（字段初值） | 是 | L2 |
| 5 | contact.address / **contact_field** (d) | 字段 `ITEM_CONTACT_FIELD="d"` ContactFilter:103→:416 | resolveRecipes `recipe("contact_field")` | 否（字段初值） | 是 | L2 |
| 6 | contact.address / **type_field** (e) | 字段 `ITEM_TYPE_FIELD="e"` ContactFilter:104→:403 | resolveRecipes `recipe("type_field")` | 否（字段初值） | 是 | L2 |

- **#3/#4/#5/#6 = 干净**：与 C5a 完全同模式（字段初值改引 `RegistryFallback` + resolveRecipes `recipe()/recipeArr()` 取值）。零回调体改动。
- ⚠️ **#1/#2 复杂度（WARN，非阻塞）**：`"n"/"m"/"s"` 不止一处——`installMvvmListHooks`（:391 L1 扫描、:447 L2 扫描）+ `installMvvmListL3Hooks`（:493 排除 L1/L2）。接 = **多点 install 改**（都在 install 期、非回调体，铁律29 不触），但需**所有出现处一致**用同一解析值，否则 L1/L3 口径漂移。建议：先抽成 install 期 resolved 字段（如 `L1_METHODS[]`/`L2_METHOD`），三处统一引用。
- 接6 的 6 个 key **全部不在 SO 自检 field_eq**（自检只钉 conv: mvvmlist_class/adapter_class/l4_notify/wxid_getters；moments: item_friend/adapter_class/actor_wxid_field；contact: item_class/contact_class/wxid_getter；search: gateway/render_hook/extractor_profile/unlock_entry/adapter_family）→ 接6 保留在 json，自检零影响 ✅。

---

## 3. 影响面（regen 链 + s_rel 不变 + DEVTEST-3AB7ED）

| 项 | 结论 | 级 |
|---|---|:--:|
| 动 `registry_8071.json` | 是（删8 子字段 + 接6 保留不动值） | L1 |
| regen `registry_cipher.inc` | **必须**：`gen_registry_cipher.py --recipe android_8071`（脚本内容无关、只 strip `_*` 顶层键后整体加密，删/接子字段不触发脚本逻辑改） | L2 |
| regen `RegistryFallback.java` | **必须**：`gen_registry_fallback.py`（删8 → 少 8 常量；接6 已在 json、本就生成 → 这 6 常量将被 Filter 新引用） | L2 |
| KDF / 派生 key | **不动**（删/接字段不碰 `derive_registry_key`/段常量/`_CERT_SHA256`） | L1 |
| s_rel / 服务器种子 | **不变** | L1 |
| 测试卡 `DEVTEST-3AB7ED` 解得开？ | **是**（KDF+nonce 重随机但 key 派生同源；cipher 用同 key 重加密 → SO 同 key 解开；删/接只改明文内容） | L3（待装机 L1 复证） |
| **删3 → SO 自检** | **必须同步删 registry_loader.cpp:314/324/338 + 重编 SO 双 ABI** | L1 |
| gen 脚本是否按 key 校验/遍历 | `gen_registry_cipher.py`=否（整体加密）；`gen_registry_fallback.py`=泛型遍历全 key（自动适配）→ **无需改脚本** | L2 |

---

## 4. 【安全与加密官 · 改前审查】结论：WARN

| # | 项 | 答 |
|:--:|---|---|
| 1 | 触碰授权根锁 | 否（不动 isVip/envelope） |
| 2 | 触碰 SO 解密 | **部分**：删3 需改 `registry_loader.cpp` 自检 `field_eq`（**仅删 3 行断言**，不动 `decrypt_config`/`derive_registry_key`/KDF）；regen `.inc`（同 key 重加密） |
| 3 | 触碰服务器 envelope | 否 |
| 4 | 触碰 LeaseClock | 否 |
| 5 | 触碰 RiskState | 否 |
| 6 | 可能误杀正版 | **删=没人读（22 真读集已证不含）；接=值逐字节一致**；唯一翻车点 = 删3 漏改 SO 自检 → 体检红/scatter（装机回归兜底） |
| 7 | 影响已验证 hook | **否**（接6 全 install 期/字段初值；缓1=回调体已排除）；删8 不碰 hook |
| 8 | 需授权检查官共审 | 是（见 §5） |
| 9 | 允许改 | `registry_8071.json`（删8子字段/接6不动）· `registry_cipher.inc`(regen) · `RegistryFallback.java`(regen) · 4 Filter resolveRecipes/字段初值/install 扫描(接6) · `registry_loader.cpp` **仅** :314/:324/:338 三行(删3) |
| 10 | 禁止改 | `derive_registry_key`/段常量/`_CERT_SHA256`/KDF · `decrypt_config` · hook 回调体（缓1 kc5.y）· s_rel/envelope/状态机/授权 |

> 真锁不削弱：删的 3 个 self-test canary 是「死字段值对账」，非密钥/签名；真锁仍 = 服务器种子 + KDF + GCM tag（自检 1c/1d 段保留）。

## 5. 【授权检查官 · 9 项】结论：PASS

| # | 项 | 答 · 证据 |
|:--:|---|---|
| 1 | 乱接状态机 | 否（不碰 StateMachine/RefreshBus） |
| 2 | 混淆入口口令/授权 | 否（unlock_entry 是 registry 描述串，删它不碰 SearchUnlock 111111 链；EntryGate 代码独立） |
| 3 | 未授权越权 | 否 |
| 4 | 绕过 AuthGate | 否 |
| 5 | 破坏 SearchUnlock→sm→Bus→Filter 链 | 否（接6/删8 不碰 SearchUnlock；SearchFilter 仅 install 期取值） |
| 6 | 影响 Java/C++ 分层 | 否（SO 改仅自检断言，仍 RiskGate/config 层；Filter 仍只读 recipe，不读 C++ sm） |
| 7 | 保护区未审先改 | 否（本报告即先审；动 registry/SO/Filter = 保护区，走本审 + 用户拍 + 快照） |
| 8 | 模块边界清晰 | 是（Filter 只拿 recipe；不需拆代码；接6 不新增 pack、归现有 4 entry） |
| 9 | 最小修复建议 | 删8 拆「删5纯json + 删3连SO自检」；接 l1/l2_methods 先抽 install 期字段再三处统一引 |

**门控**：Entry ✅ / Auth ✅ / State ✅ / Risk ✅（自检 canary 减 3，真锁段 1c/1d 不动）/ 分层 ✅ → **PASS**（授权面）。
**安全面综合 = WARN**（删3 的 SO 自检同步是必做项，漏做即体检红）。

---

## 6. DoD（落地完成定义）

1. regen 后装机用 `DEVTEST-3AB7ED` 完整链：`recipeOk=true` + `registrySummary ... entries=4` + `registry_self_test` 不 FAIL（删3 已同步）。
2. 密友隐藏正常（删/接都不误伤：删=无人读、接=值一致 → 朋友圈/会话/通讯录四链照常）。
3. jadx/strings 扫 release：不新增明文（删=更少；接6 release 仍 fail-closed ""）。
4. worklog 落 L1 原文 + 更新任务卡 §8。

## 7. 诚实边界 / 待确认

1. `#2 conv.list/contact_class`(l4)：删前 L4 再复核有无**反射按全限定名加载**（本次 grep 仅覆盖 recipe/字面量，未穷举反射动态拼名）。
2. `DEVTEST-3AB7ED 仍解得开` 标 L3：KDF 同源推断；以装机 L1（recipeOk=true）为准。
3. 本审 L2 静态（源码 file:line + grep）；未装机/Frida 动态验。落地仍走任务卡 §5「快照 → 改 → 装机回归」。
4. 未核 `tools/gen_registry_cipher.py` 之外的 cpp 是否另引这 8 key（已核 `registry_loader.cpp` 全自检段；`config_crypto.cpp` 不读 registry 内容键，只管 KDF/解密）。

## 8. 本次未做（守只读红线）

- 未改任何代码 / `registry_8071.json` / SO / 生成脚本 / 既有文档正文。
- 未执行 git 写动作；未做实际删/接/缓。

# End（C7 改前双审 · 只读 L2；删8=删5纯json+删3连SO自检〔registry_loader.cpp:314/324/338〕；接6 干净〔l1/l2_methods 多点 install 注意〕；缓1 不动；KDF/s_rel 不变、DEVTEST-3AB7ED 应仍解〔L3 待装机证〕；综合 WARN）
