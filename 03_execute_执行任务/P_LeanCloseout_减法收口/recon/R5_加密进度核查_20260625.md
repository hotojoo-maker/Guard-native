# R5 · 加密进度核查（只读侦察）

> **角色**：Guard Native 只读侦察 worker。未改任何代码 / 配置 / 他处文档；本文件为唯一新建产物。  
> **日期**：2026-06-25  
> **漂移回正（2026-06-25）**：§⑤ 列出的 1~4 项已在同日落盘修正（`PROTECTION_MAP §10.6/§10.7`、`R3` TL;DR、`EncryptedConfigLoader` 注释、安全官 skill pv）；§⑤ 保留侦察当时快照供对照。
> **二次复核回正（2026-06-27 · 会话 F37 · 对代码逐条核、不只信文档）**：主线结论与代码一致，**65% 成立**（牙①②✅ / 牙③🟡双试·全局W仍在 / 牙④⬜·unwrap 无 expire 检查 / D8 release 兜底空但内联 5 处仍在）。新增 3 处小漂移（非功能）见 §⑤ #7~#9，已分别回正进本文 / `P_RB1/worklog` / `PROTECTION_MAP §10.10`。
> **三次回正（2026-06-27 晚 · 会话 G91 · git + 服务器 REVIEW 对码）**：F37 之后当日又落码——**牙④ a 案已落**（`config_crypto.cpp` `seed_expired`/`set_envelope_expiry` + `NativeBridge.setEnvelopeExpiry`，commit `f6cc31b`/`c5597b1`）；**牙③ Batch2 服务器侧已部署主节点**（`I:\miyou-server\REVIEW_2026-06-27.md` §A：prod KDF 自检过 + 冒烟全绿，灰度② dm 回填 ~25%）。故下方 §② `P1F-W`/`P1G`「🔴 待立项」、§③ `E9`「待立项」均为侦察当时快照、**已不成立**（见各行就地回正）；删全局 W（Batch3）真卡点 = dm 回填 ≥90% + 观察窗（REVIEW §B），非预算。
> **输入**：任务卡 §3/§4/§8、`worklog.md`、`PROTECTION_MAP.md` §10.6/§10.7/§10.9、网络安全官 skill §阶段计划、已有 `recon/R1~R4b`、磁盘 L1 日志（见下表）。  
> **证据口径**：L1=装机/logcat/Frida 原文；L2=读码/反编译/构建脚本；L3=多信号收敛；L4=待验；❓=文档间张力。

---

## ① 一句话总结论

**加密主线已到「prod_server_lock + Filter 读 registry + C5a 兜底归一」中段（约 65%）**：有 server seed 时 `recipeOk=true` / 密友隐藏已 L1 实证；**真锁终局未达**——release 仍有 **5 处内联 hook 明文**（C5b 旧账）、**W 全局静态明文仍在**（牙③ batch0/1 已落 + Batch2 服务器已部署主节点 `06-27`、灰度② dm~25%；删全局 W=Batch3 待 dm≥90%）、**D8 删 fallback 六缺口全未闭**；**P1G（牙④）已落码 `06-27`**（旧文「仅设计」已过时，G91 回正）。

---

## ② 主线 phase 表（加密链 A 线）

| 项 | 状态 | 等级 | 证据 |
|---|---|:--:|---|
| **P1A** `decrypt_config` AES-GCM + JNI | ✅ 完成 | L1 | `c5a_install_20260625.log` / `c5a_authverify_20260625.log`：`BATCH1_VERIFY PASS`、`PHASE1A_VERIFY PASS` |
| **P1B** Registry 抽取（4 gateway） | ✅ 完成 | L1 | 种子后 `registrySummary … entries=4`（`c5a_authverify_20260625.log:20,43`）；冷启动无种子 `PHASE1B_VERIFY FAIL`（预期，`configReady=false summary=scatter`，`:18`） |
| **P1C** Encrypted registry（`registry_cipher.inc`） | ✅ 完成 | L2+L1 | L2：`registry_cipher.inc` `GUARD_REGISTRY_REQUIRES_SERVER_SEED=1`；L1：种子后 PHASE 链 B~E 在 auth 路径 PASS（`c5a_authverify_20260625.log:55`） |
| **派生 key + 证书绑定**（1D-local A-step2） | ✅ 完成 | L2 | `config_crypto.cpp:343-347` `g_binding` + `set_binding_material`；`ModuleMain.bindSigningCert()` 读模块 APK 签名折 key（V3 改宿主源 **未做**，见漂移） |
| **P1D-server** S2 出站 / S3a prod_server_lock / S4 Ed25519 / S3b LeaseClock | 🟡 部分 | L1+L2 | L1：`cached envelope seed=ok` → `recipeOk=true`（`c5a_authverify_20260625.log`）；L2：`EnvelopeStore.getVerifiedCachedEnvelope()` 每次 Ed25519 重验（`:152-168`）。**未完成**：删 fallback、V3 发行线、RiskState 真散沙（`PROTECTION_MAP §10.6` 自述） |
| **P1E** Filter 读 registry（`GuardRuntime.getRecipe`） | 🟡 部分 | L2+L1 | L2：Conv/Contact/Moments/Search 均经 `RegistryFallback` + `getRecipeOrFallback`；release `RegistryFallback` 全 `""`（`src/release/.../RegistryFallback.java`）。L1：四 Filter `fallbackSelfTest=ok`（`c5a_install_20260625.log:5-8`）。**删 fallback 未做**：5 处内联明文仍存（见四问②） |
| **P1F** LeaseClock + RiskState | 🟡 部分 | L2 | L2：`LeaseClock.trustedNow()` 用于到期（`EnvelopeStore:137`）；`RiskState` 主链偏 record-only（`PROTECTION_MAP §10.6:283`）；来电散沙例外已接（§10.9） |
| **P1F-W** W 一机一密 | 🟡 灰度② | L2(06-27) | 回正：batch0/1 已落码（`caf2142`）+ **Batch2 服务器已部署主节点**（`REVIEW_2026-06-27.md` §A，dm 回填~25%）；`g_wk_lo`/`g_wk_hi` 仍在（删=Batch3，待 dm≥90%）|
| **P1G** 重放绑定（牙④ a 案 · 不折静态 key） | ✅ 已落码 | L2(06-27) | 回正：a 案已落（`seed_expired`/`set_envelope_expiry`+`NativeBridge.setEnvelopeExpiry`，commit `f6cc31b`/`c5597b1`）；`unwrap_server_seed` 已比 `expire_at<=trusted_now` 过期散沙（原「摘要折 key」已否决）|

---

## ③ 减法收口表（任务卡 §8 总图）

| 步 | 内容 | 状态 | 等级 | 证据 |
|---|---|:--:|:--:|---|
| **C5a** | 4 Filter 22 兜底 → 构建期 `RegistryFallback` | ✅ 完成 | L1 | `worklog.md` C5a 节；`c5a_install_20260625.log` fallbackSelfTest×4；release 10/15 标记 absent（worklog §③）；`c5a_authverify_20260625.log` recipeOk=true |
| **C7-删8** | 删 registry 8 挂空键 37→29 | ✅ 完成 | L1+L2 | L2：`registry_8071.json` 已无 `l4_notify`/`list_addall`/`policy` 等（对照 R2b 删8 清单）；L1：`worklog.md` C7 节 `[CF:L4] cleaned=4`、recipeOk=true（log 名 `c7del8_recipeok_20260625`，worklog 引用；**磁盘 `logs/` 副本本次读权限 denied，以 worklog 摘录为准**） |
| **C7-接6** | 6 字段内联 → registry | 🟢 低风险待做 | L2 | R2b 清单：`l1_methods`/`l2_method`/`contact_fields`/`actor_field_names`/`contact_field`/`type_field`；代码仍内联（例 `ContactFilter:103-104` `ITEM_*_FIELD="d"/"e"`） |
| **C6** | Conv/Search 安装期锚点收 registry；Push 延后 | 🟡 部分/待做 | L2 | Search 已走 C5a `RegistryFallback`；**PushFilter 零 `getRecipe` 引用**（grep 无匹配）；Search 脆点 `z15.ef6` 等仍可能内联（R2 §B 类，未逐条复核） |
| **C5b+C7缓1** | 回调体内 `kc5.y` 等 | 🟠 缓 | L2 | R2b #1 `conv.list/item_class` 在 ConvFilter 回调体内；铁律29 单独回归 |
| **E9 W_dev** | 修 W 全局静态「破一次通杀」 | 🟡 灰度② | L2(06-27) | 回正：已立项=P_RB1 牙③；batch0/1 已落 + Batch2 服务器部署主节点；删全局 W（Batch3）待 dm≥90%（`REVIEW_2026-06-27.md` §B）|
| **D8** | 删 release 明文 fallback | ⛔ 阻塞 | L2+L3 | R3 + 任务卡 §8：六缺口未闭；C5a 只清 22 兜底常量，**未删内联 + 未删 debug fallback 机制** |

---

## ④ 四问回答（PROTECTION_MAP §10.7 P0 口径）

### Q1：无 server seed 时 `registrySummary` 是否 scatter、`isConfigReady` 是否 false？

| 结论 | **是（fail-closed）** |
|---|---|
| 等级 | **L1 + L2** |
| 证据 | L2：`registry_loader.cpp:194` `GUARD_REGISTRY_REQUIRES_SERVER_SEED && !server_seed_ready()` → scatter；`EncryptedConfigLoader:64` `sReady = !"scatter".equals(summary)`。L1：`c5a_install_20260625.log:12,18` 无种子 `registrySummary=scatter`、`configReady=false`；`c5a_authverify_20260625.log:29-35` 冷启动同理，种子后 `:43` entries=4 recipeOk=true |

### Q2：release 包还能搜到核心 hook 类名/字段（明文 fallback 残留几处、在哪）？

| 结论 | **C5a 22 兜底已清；仍 **5 处** 内联/非兜底明文**（C5b/D8 旧账） |
|---|---|
| 等级 | **L1（C5a 扫描）+ L2（源码定位）** |
| 证据 | L1：`worklog.md` / `c5a_install_20260625.log` §③ release strings 扫描 — 5 present：`MvvmList`（`ContactDiscoveryHook.java:459`）、`fc5.g`/`z3`（`ContactLabelHideGuard.java:31/35`）、`LikeUserList`/`CommentUserList`（`MomentsFilter.java:66-67` 常量 `*Count` 子串）。L2：release `RegistryFallback.java` 29 常量全 `""` — **兜底层已 fail-closed** |

### Q3：W wrapping key 现在是不是还全局静态明文（`config_crypto.cpp`）？

| 结论 | **是，仍是全局静态 `g_wk_lo`/`g_wk_hi` 明文常量** |
|---|---|
| 等级 | **L2** |
| 证据 | `native_core/src/config_crypto.cpp:360-361`；`P_RB1/钥匙加固_KeyHardening设计.md:31` 状态 ⬜ 设计 only |

### Q4：删 Filter fallback（D8）的 6 个前置缺口闭了几个？

| 缺口 | 状态 | 说明 |
|---|:--:|---|
| **G1** C5~C7 落地 | 🟡 **部分 2/4+** | C5a ✅、C7删8 ✅；C7接6 ⬜、C6 ⬜、C5b缓1 ⬜ |
| **G2** cert 轴 per-release | ⛜ **0** | `_CERT_SHA256` / `CompatProbe.EXPECTED_CERT` 仍硬编码 `ca421ec3…`（R3 §2.1 L2） |
| **G3** V3 证书源切换 | ⛜ **0** | 仍读模块 APK 签名（R3 §2.2 L2） |
| **G4** `release_id`/包名 flavor 注入 | ⛜ **0** | `AppConfig.java:50` 硬编码 `android_8071`；gradle 只注 `GUARD_WX_PKG`（L2） |
| **G5** V3 重签包 registry 解开 L1 | ⛜ **0** | 无 L1 收据（L4 待补） |
| **G6** 删 fallback 回归垫 | ⛜ **0** | D8 未动 |

**合计：六缺口无一闭合；G1 子步约完成 2 项（C5a、C7删8）。D8 仍 ⛔ 不可解冻。**

---

## ⑤ 发现的漂移（文档 vs 代码/日志）

| # | 文档说的 | 实际 | 级 |
|:--:|---|---|:--:|
| 1 | `PROTECTION_MAP §10.6` `AppConfig` 行写 `GUARD_PRODUCT_VERSION=v1.1` | `build.gradle:28` / `versionName "1.3"` / `GUARD_PRODUCT_VERSION "v1.3"` | L2 |
| 2 | `recon/R3_V3现状_D8前置.md` TL;DR「C5 仅只读核账未落地」 | C5a + C7删8 已改码 + L1（06-25 worklog） | L2 |
| 3 | `EncryptedConfigLoader.java:20-21` 注释「No server lease wired yet」 | 实际 server seed 经 `EnvelopeStore.applyCachedEnvelopeSeed` → `NativeBridge.applyServerSeedAndReset` 折 key（`registry_loader.cpp:194`） | L2 |
| 4 | `PROTECTION_MAP §10.7 P0-2`「`isAuthorizedNow` 不能只看 tk/bl/le」 | 现 `getVerifiedCachedEnvelope()` 每次对 blob 重跑 `AuthEnvelopeVerifier.verifyAndParse`（`EnvelopeStore:152-168`）——**部分已修，负向 L1（手写 tk/bl/le）待补** | L2 |
| 5 | 任务卡 §8 写 pv 真值 v1.3 vs `§10.6` 仍 v1.1 | 客户端 L2 已是 v1.3；服务器 envelope `pv` 是否同步 **L4 待验** | L2/L4 |
| 6 | 安全官 skill「P1E 删 fallback 未做」 | 与 C5a 一致：release 兜底空，但内联 5 处 + D8 未删 → **口径一致，非漂移** | — |
| 7 | `config_crypto.cpp:357/416/557` 注释「Batch0 仅存储 / 未接 unwrap」 | `unwrap_server_seed:448-468` 实际已双试 W_dev（功能=Batch1）；**注释滞后**（.cpp 属 SO 源，改注释待用户点头） | L2(06-27) |
| 8 | `P_RB1/worklog`(06-26)「客户端块B markRefunded 待落」 | 客户端链已落码：`EnvelopeStore.markRefunded():193` + `applyVerifiedEnvelope` rf=1→撤 `:107` + `isAuthorizedNow` 查 `isRefunded():147` + `AuthEnvelopeVerifier.rf:62`；**服务器 rf push 仍暂缓未建**（`EnvelopeStore:172` 字段先约定） | L2(06-27) |
| 9 | 防破解总账未记 D-018(06-26) | A2 防封门改吊本地完整性 cert（`CompatProbe.isIntegrityIntact:105`）、不再吊授权/server seed（`GuardRuntime.java:116-119`）；真源 `DECISION_LOG.md D-018`，已补指针入 `PROTECTION_MAP §10.10` | L2(06-27) |

---

## ⑥ 吃不准 / 待验（L4）清单

1. **`logs/c7del8_recipeok_20260625.log`**：worklog 有 `[CF:L4] cleaned=4` 摘录，磁盘 `logs/` 副本本次 **Permission denied**，未独立复核全文。
2. **共存版** `PACKAGE_MISMATCH` / registry 解开：R3 记 G6 共存包名注入 L2 应闭合，**无共存装机 L1**。
3. **手写 `tk/bl/le` 负向验收**：§10.7 P0-5 要求；**无本次磁盘 L1 日志**（代码路径已加重验，L2）。
4. **服务器 `pv=v1.2` 全局常量 vs 客户端 v1.3**：任务卡 §8 记接点①待修，**未查 live server**（L4）。
5. **SearchFilter 脆锚点**（`z15.ef6`/`fz2.e`）：C6 前是否仍内联 — R2 有账，本次未逐行 grep（L4 细项）。

---

## ⑦ 引用日志索引（L1 磁盘文件）

| 文件 | 用途 |
|---|---|
| `P_LeanCloseout/c5a_install_20260625.log` | C5a debug recipes + 无种子 scatter + release 扫描 |
| `P_LeanCloseout/c5a_authverify_20260625.log` | 授权全链 recipeOk=true + 冷启动 PHASE 顺序说明 |
| `P_LeanCloseout/worklog.md` | C5a/C7删8 装机摘要 + C7 cleaned=4 |

---

*侦察完成。未改代码。下一刀建议（非指令）：C7-接6（R2b 六字段）→ C6 Search 三证 → E9 单独立项；D8 仍等六缺口。*
