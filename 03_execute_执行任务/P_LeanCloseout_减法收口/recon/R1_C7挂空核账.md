# R1 · C7 挂空核账（registry 字段「定义但没人读」盘点）

> 角色：Guard Native 只读侦察 AI（**只读、未改任何代码 / 未改别处文档正文**；本文件 = 唯一新建产物）
> 出具：2026-06-25 · 任务来源：`P_LeanCloseout_减法收口/任务卡.md` §4 C7
> 方法：① 列 `native_core/registry_8071.json` 全 `entry.key`；② grep 全仓 `getRecipe / getRecipeOrFallback / getRecipeListOrFallback` 及各 Filter 的 `recipe()/recipeArr()` 包装；③ 交叉比对。
> 成色：读取/挂空划分 = **L2 静态实证**（源码 file:line）；个别消费点未现场确认处标 **L4**。
> 红线遵守：只读 · 不改代码 · 不改别处文档 · 结论标级 · G1 不猜（吃不准标 L4，不写成结论）。

---

## 0. 一句话结论（L2）

`registry_8071.json` 共 **37 字段**；经 `getRecipe*` 真读 **22**（真接线），**15 挂空**（定义了但没有任何 `getRecipe*` 读它）。
数字对账：**37 = 22 真接线 + 15 挂空** ✅（与安全官 skill「37 字段 / 22 真接线 / 15 挂空」口径一致）。

挂空 15 个再分三桶：**接上 4 · 有意内联(铁律29)受限 3 · 建议删 8**。

---

## 1. registry 全字段（37）· task ①

| 动脉 | 字段（key） | 值 |
|---|---|---|
| **conv.list**(9) | mvvmlist_class | com.tencent.mm.plugin.mvvmlist.MvvmList |
| | adapter_class | kc5.v0 |
| | item_class | kc5.y |
| | contact_class | com.tencent.mm.storage.l4 |
| | l1_methods | n,m |
| | l2_method | s |
| | l4_notify | notifyDataSetChanged |
| | contact_fields | d,e,f,a,b,c |
| | wxid_getters | C0,h1,j1,i1,k1,getUsername,getUserName |
| **moments.feed**(12) | item_friend | na4.b |
| | item_promo | la4.p |
| | adapter_class | e2 |
| | wxid_field | field_userName |
| | inner_field | d |
| | sns_getter | h1 |
| | list_addall | ArrayList.addAll,LinkedList.addAll |
| | like_list | LikeUserList |
| | comment_list | CommentUserList |
| | actor_class | z15.e56 |
| | actor_wxid_field | f435583d |
| | actor_field_names | d,f435583d,username,field_userName |
| **contact.address**(9) | live_list | …address.AddressLiveList |
| | mvvmlist_class | …mvvmlist.MvvmList |
| | adapter_class | ik3.t0 |
| | item_class | fc5.g |
| | contact_class | com.tencent.mm.storage.z3 |
| | contact_field | d |
| | type_field | e |
| | wxid_getter | c1 |
| | list_addall | ArrayList.addAll |
| **search.gateway**(7) | gateway | fts_result_view |
| | adapter_family | q2,f0 |
| | render_hook | getView |
| | policy | hide_if_target_in_hidden_union |
| | extractor_profile | wechat8071_fts_mixed |
| | scope | result_render_only |
| | unlock_entry | excluded_search_unlock_111111 |

---

## 2. 真接线（22）· task ②（被 `getRecipe*` 真读的字段 + file:line）

> 各 Filter 用本地 `recipe(key,fallback)`/`recipeArr(key,fallback)` 包装，gateway 固定，key 为字面量；下表是 key 字面量出现处。

| 动脉 | 真读字段（22） | 读取点 file:line |
|---|---|---|
| conv.list(3) | mvvmlist_class / adapter_class / wxid_getters | ConvFilter:112 / 113 / 114(recipeArr) |
| moments.feed(8) | item_friend / item_promo / adapter_class / wxid_field / inner_field / sns_getter / like_list / comment_list | MomentsFilter:92 / 93 / 94 / 95 / 96 / 97 / 98 / 99 |
| contact.address(6) | adapter_class / live_list / mvvmlist_class / item_class / contact_class / wxid_getter | ContactFilter:79 / 80 / 81 / 82 / 83 / 84 |
| search.gateway(5) | gateway / adapter_family / render_hook / extractor_profile / scope | SearchFilter:94 / 95 / 96 / 97 / 98 |

**自测/探针额外直读（非过滤主链，仅验活）**：`conv.list/adapter_class` + `search.gateway/gateway` 各被 `GuardHeartbeat:111-112`、`DebugServer:184-185`、`ModuleMain:415-416` 直读；`ModuleMain:417-418` 为故意 miss 负向探针（`no.such.gateway` / `no_such_field`）。

---

## 3. 挂空（15）· task ③（定义了但没人 `getRecipe*` 读 → 建议接上/删）

| # | 动脉 / 字段 | 被谁读 | 内联是否在用（file:line） | 建议 | 级 |
|---|---|---|---|---|---|
| 1 | conv.list / **item_class** (kc5.y) | 没人读 | 在用，且在 hook 回调体内：ConvFilter:352,408,573,1071；ConvHotReload:374,424 | **保留内联**（铁律29 禁动回调体）→ 归 C5 评估能否在 install 期 resolve；C7 不擅动 | L2 |
| 2 | conv.list / **l1_methods** (n,m) | 没人读 | 在用，install 扫描循环：ConvFilter:384,388,486,627,631 | **接上候选**（install 期 resolve，比回调体风险低）→ C5 带快照+回归 | L2 |
| 3 | conv.list / **l2_method** (s) | 没人读 | 在用，install 扫描：ConvFilter:437,440,486,645 | 同上 **接上候选**（C5） | L2 |
| 4 | conv.list / **l4_notify** (notifyDataSetChanged) | 没人读 | 在用，但是 **JDK 方法名**：ConvFilter:822,985 等多处 | **删**（JDK/框架方法名，非混淆名，进 registry 无防护价值） | L2 |
| 5 | conv.list / **contact_class** (com.tencent.mm.storage.l4) | 没人读 | 仅注释 ConvFilter:35；未见 live 代码按类名引用 | **删**（无 live 锚点；如担心遗漏，迁前 L4 复核一次） | L2/L4 |
| 6 | conv.list / **contact_fields** (d,e,f,a,b,c) | 没人读 | 在用，内联数组 `CONTACT_FIELD_NAMES` ConvFilter:72 → 消费 :1981,2015,2180,2257,2272 | **接上**（仿 wxid_getters 用 `recipeArr("contact_fields")`，归一） | L2 |
| 7 | moments.feed / **list_addall** (ArrayList/LinkedList.addAll) | 没人读 | 在用，但 **JDK 方法**：MomentsFilter:141-153 直接 `ArrayList.class.getMethod("addAll")` | **删**（JDK 方法，非混淆名） | L2 |
| 8 | moments.feed / **actor_class** (z15.e56) | 没人读 | 代码注释自述 dead：MomentsFilter:43-44「z15.e56 … NOT migrated — no live anchor / dead constant」 | **删**（dead constant，代码已自证） | L2 |
| 9 | moments.feed / **actor_wxid_field** (f435583d) | 没人读 | 值在别处复用：MomentsFilter:67(FIELD_E56_WXID),:73(数组内);MomentsRedDotGuard:130；注释:43-44 称未迁 | **删**（与 actor_field_names 重复；单独 key 冗余） | L2 |
| 10 | moments.feed / **actor_field_names** (d,f435583d,username,field_userName) | 没人读 | 内联数组定义 MomentsFilter:71-73；本次未现场确认其消费点 | **接上候选**（`recipeArr`），但**迁前先确认消费点** | L4 |
| 11 | contact.address / **contact_field** (d) | 没人读 | 在用，内联 `ITEM_CONTACT_FIELD="d"` ContactFilter:103 → :416 | **接上**（`recipe("contact_field")`，归一） | L2 |
| 12 | contact.address / **type_field** (e) | 没人读 | 在用，内联 `ITEM_TYPE_FIELD="e"` ContactFilter:104 → :403 | **接上**（`recipe("type_field")`，归一） | L2 |
| 13 | contact.address / **list_addall** (ArrayList.addAll) | 没人读 | 在用，但 JDK 方法：ContactFilter:128,178,243 | **删**（JDK 方法，非混淆名） | L2 |
| 14 | search.gateway / **policy** (hide_if_target_in_hidden_union) | 没人读 | 无内联引用（DebugServer:307 读的是 HTTP body 的 "policy" 参数，与 registry 无关） | **删**（行为描述串，非代码锚点；如要留作说明，移注释，别挂 registry 半截） | L2 |
| 15 | search.gateway / **unlock_entry** (excluded_search_unlock_111111) | 没人读 | 无内联引用 | **删**（标记/描述串，非代码锚点） | L2 |

---

## 4. 分三桶小结（接上 4 · 有意内联/受限 3 · 删 8）

- **桶 A｜接上（归一，4）**：`conv.list/contact_fields`、`contact.address/contact_field`、`contact.address/type_field`、`moments.feed/actor_field_names`(消费点待 L4)。
  - 共性：值是真混淆/字段锚点，已有内联明文副本 → 用 `recipe()/recipeArr()` 接上、删内联副本即归一。`contact_fields`/`actor_field_names` 仿 `wxid_getters`(ConvFilter:114) 走 `recipeArr`。
- **桶 B｜接上但受铁律29 约束（3）**：`conv.list/item_class`、`conv.list/l1_methods`、`conv.list/l2_method`。
  - `item_class(kc5.y)` 用在 **hook 回调体内**（铁律29 禁动）→ 本轮**不动**，C5 评估能否改在 install 期取值。
  - `l1_methods/l2_method` 用在 **install 扫描循环**（非回调体）→ 接上风险较低，但仍属已验证 Filter，C5 须「快照+双审+装机回归」。
- **桶 C｜建议删（8）**：
  - JDK/框架方法名（非混淆名，进 registry 无防护价值）：`conv.list/l4_notify`、`moments.feed/list_addall`、`contact.address/list_addall`。
  - dead / 冗余常量：`moments.feed/actor_class`(代码自证 dead)、`moments.feed/actor_wxid_field`(与 actor_field_names 重复)、`conv.list/contact_class`(仅注释，L4 复核)。
  - 纯描述串（非代码锚点）：`search.gateway/policy`、`search.gateway/unlock_entry`。

> 桶 B/C 的「删 / 接上」都会动 `registry_8071.json`（→ `gen_registry_cipher.py` → SO），属改前审范围；**本卡 C7 只登记建议，不动手**（落地走任务卡 §5 改前必审 + git 快照）。

---

## 5. 诚实边界 / 待确认（L4）

1. `moments.feed/actor_field_names`（#10）：内联数组定义点已见（MomentsFilter:71-73），**消费点未在本次 grep 确认**；标 L4，接上前需确认是否真用于 D2/D3 提取，避免删/迁误伤已验证朋友圈点赞评论过滤。
2. `conv.list/contact_class`（#5，l4）：仅注释引用，**无 live 代码按类名用**为本次 grep 结论（L2）；删前可再 L4 复核一次有无反射按全名加载。
3. 本核账只覆盖 `*.java` 的 `getRecipe*` / `recipe()` 读取与内联字面量；未核 `native_core/*.cpp`、`tools/gen_*_cipher.py` 是否另行引用这些 key（如它们生成/校验逻辑按 key 遍历，则「删」需同步改生成脚本 → 归 C5/改前审）。

---

## 6. 本次未做（守只读红线）

- 未改任何代码、未改 `registry_8071.json`、未改任务卡/其他文档正文。
- 未执行 git 写动作。
- 未对桶 B/C 做实际删/接上（那是后续带快照+双审的落地步骤）。

# End（C7 挂空核账 · 只读 L2；接上 4 / 受限 3 / 删 8；落地前走任务卡 §5 改前审 + git 快照）
