# P26C 工作日志

## 2026-05-24 探针归档
- `field_labelID` / `field_labelName` 已定位（父类 `rl.g2`）
- addAll 时机 ID=-2000000、name=null → 过滤 hook 不能直读 addAll
- `installLabelListProbe` + `/api/labels` + Web 标签卡片已通
- 归档 → `result.md`

## 当前卡点
🟡 指定标签 **remove hook 待写**（需绕过 WCDB lazy load）

## 下一步
Web UI 多选隐藏 ID → Bridge → bind 层或探针缓存过滤 → 装机验收路径 C
