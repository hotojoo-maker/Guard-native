# 任务卡 · P_HotUpdateFreeze 官方热更新冻结

> 建卡：2026-06-30。完成：2026-07-01（V1.6 装机验证通过）。
> 防御性：冻结官方热更新通道，防静默推新检测 / 改动我方 hook 依赖类 / 整包更新换掉重打包版本。
> 真源（只链不复制）：`C:\Users\Me\Desktop\防封_反检测线\热更新支线_2026年6月.md` + `证据\HOTUPDATE_8071_20260622.md`

状态: ✅ 已落码 + 装机验证（官替 com.tencent.mm + 共存 com.tencent.mn，8.0.71，2026-07-01）

---

## 0. 与现有 B7 的关系

| 模块 | 职责 |
|------|------|
| `UpdateGuard.java`（B7） | UI 层：断 `fl4.o` 红点 getter（`[UG]`） |
| `HotUpdateFreeze.java`（本卡） | 通道层：Tinker / 整包更新 / libcso 三通道（`[HUF]`，开关 `AppConfig.isHotFreezeEnabled` **默认开**） |

---

## 1. 目标（一句话）

主进程纯 Java no-op 官方三条热更新通道 = 预防性冻结（生产默认开，锁版本）。

---

## 2. 三条通道（jadx 8.0.71 实读 L2 + 装机 L1）

| 通道 | hook 点 | 动作 | 状态 |
|------|---------|------|------|
| ① Tinker 静默补丁 | `p53.j.b(Map)` 查更（叠 `m53.d0.j`/`m53.d0.d`） | freeze no-op，断查更 | ✅ L1 blocked |
| ② 整包客户端更新 | `fl4.o.Wg(boolean,boolean,boolean)` 查更 + `fl4.o.Bg(Context,String)` 装包弹窗 | freeze no-op | ✅ L1 blocked（点检查更新不再后台下载） |
| ③ libcso SO 补丁 | `ip.g.a` | **仅观测不拦**（libcso 兼正常 SO 加载，整冻误伤；远程段 G3 未定位） | ⚠️ observe-only |

**禁区**：libcso C11（native mprotect）、任何重碰 native（F-23 不重碰）。

---

## 3. 落地约束（已遵守）

- 仅主进程；`:push` 不装（铁律 6）
- hook 后于 `nativeInit`，与 `ModuleMain` 业务 hook 同批；F-25 `catch(Throwable)`
- 字段写法（`z.f176124s` / `g45.c.f246162e` 官方自带闸）运行时 NoSuchField（Tinker classloader 分裂）→ 已弃，走方法钩
- A2×Tinker：A2 spoof 官方签名后 Tinker 验签可能重开 → 上 A2 必冻 Tinker（已默认冻）
- LSPatch 共存零副作用：模块在 base.apk、非 tinker patch，断新 patch 不卸模块

---

## 4. DoD（全部完成）

1. [x] `brief.md`：D3 精确 hook 点 + no-op + 与 UpdateGuard 分工
2. [x] 8.0.71 实机 L1：官替 `p53.j.b`→blocked、`fl4.o.Wg`→blocked，微信正常登录用（G7）
3. [x] 主线落码：`HotUpdateFreeze.java` + `ModuleMain` 接入 + `AppConfig.isHotFreezeEnabled`（默认开）
4. [x] `result.md`：L1 装机日志（官替 + 共存）

---

## 5. 诚实口径

机制 L2 坐实、通道存在 L2；**无 L1 证明**官方已经此推过新检测 → 预防性冻结，不写「不冻必被打」。

---

## 6. 待补（动态 / 设备，低优）

- G3：libcso 远程段精准冻（探针 `recon/probe_libcso_remote.js` 已就绪；需服务器真推或新装触发才抓得到真下载）
- G4：libmeminfo ← libcso 因果

---

## 7. 交付文件

`worklog.md` · `brief.md` · `result.md` · `logs/` · `recon/`（cso 探针 + frida runner）
