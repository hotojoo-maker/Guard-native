# P26B 工作日志

## 当前卡点
⬜ 待实现 — 缺 timeline item **snsId 字段** L1 证据；UI 选帖方案未定。

## 已验证事实（✅）
| 事实 | 证据 |
|------|------|
| （无） | — |

## 当前假设（❓ 未验证）
| 假设 | 依据 | 验证方法 |
|------|------|----------|
| 可复用 D1 `addAll(na4.b)` 层 filter 自有帖 | D1 已装机 ✅ | Frida 探针读 la4.p 全字段 |
| snsId 在 la4.p 或 na4.b 某字段 | 竞品/惯例 | 爬虫 + 点自己帖时 dump |

## 下一步（只列一步）
跑 Frida 探针：打开朋友圈 → 定位自己最新一条帖 → dump `na4.b` / `la4.p` 字段，找 snsId 候选。
