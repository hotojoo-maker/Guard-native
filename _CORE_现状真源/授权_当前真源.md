# 授权 · 当前真源（Guard Native）

> 单一用途：**维护授权时只读这一页**。要改授权代码 → 先过 `授权检查官(auth-review)` 审。
> 状态：2026-07-01 立，已由 架构师 + 授权核验 两窗口交叉验证（逐条核到代码）。
> 深链（要挖细节才点）：门控裁决 `docs/GUARD_GATE_TRUTH.md` · 语义/白名单 `.cursor/skills/guard-auth-review_授权检查官/SKILL.md` · 服务器真锁现状 `PROTECTION_MAP.md §10.6`

## ① 大白话

- **授权** = 用户花钱买、服务器发的「使用资格」（绑定 wxid + 设备）。没授权 = 模块装了也不干活（功能灰、密友不藏）。
- 授权 **≠** 口令 `111111`（那只让「设置入口」重新冒出来）；**≠** 显形/隐藏（那是状态机管「现在藏不藏」）。三者各管一摊、别混——这是过去维护犯晕的头号原因。
- **服务器能远程「撤」**：单卡封停/删卡一下发，这台**立刻**断隐私+防封（可被有效授权恢复）。整批版本停用见 §⑥ F.4（注意：当前**不是**即时）。

## ② 五层门控（任一层判停 → 下游放行；互相独立、不可互相替代）

| 门 | 管什么 |
|---|---|
| 入口门 EntryGate | 口令 111111 → 只让「设置入口」可见 |
| 授权门 AuthGate | wxid + 设备 + license → 能不能用功能 |
| 配方门 ConfigGate | 服务器种子解开 registry → release 下敏感隐藏链才生效 |
| 状态门 StateGate | HIDDEN/VISIBLE → 密友过滤生不生效 |
| 风险门 RiskGate | 包/签名被改 → 全静默（SAFE_MODE） |

## ③ 代码现状（逐字核到代码 · 2026-07-01 · 两窗口交叉验证）

| 符号/机制 | 现状 | 落点 |
|---|---|---|
| `isActive()` | 四层全 AND = 授权门 && 配方门 && 密友总开关 f1 && HIDDEN 态 | `core/StateMachine.java` |
| `isVipAuthorized()` | 真授权门 = `EnvelopeStore.isAuthorizedNow()`，**非 return true** | `core/StateMachine.java` |
| 封停/删卡撤销（rf · D-020） | `isAuthorizedNow()` **第一关**查 `isCardRevoked()`；服务器 `rf=1` → 立刻撤隐私 + A2；收有效信封可恢复；篡改不走此路（不可逆） | `net/EnvelopeStore.java` |
| 服务器出站 | 卡密→token、token→签名信封；主备 fallback、强制 https、连不上=fail-closed | `net/EnvelopeClient.java` |
| 验签 | Ed25519（客户端只内置公钥）+ 设备/schema 校验；拒 HS256/无签名 | `net/AuthEnvelopeVerifier.java` |
| 本地缓存 | token / 信封 / license到期 / pv / 更新 + 租约·风控(tr/rk)·封停(rf)·续费预警(rd) 元数据；**不存密友数据** | `net/EnvelopeStore.java` |
| 可信时间 | 服务器授时 trustedNow + 防回拨；断网>72h 进设置页强制重验，失败撤销（留 token 自愈） | `core/LeaseClock.java` + `net/GuardHeartbeat.java` + `moduleB/SettingsEntry.java` |
| 官方对时（第二源） | 已落地：读官方时间抬可信水位 + 防 2 年未来值篡改 | `core/LeaseClock.noteOfficialTime` |
| 评估 | wxid + 设备 → AUTH_OK / MISMATCH / NO_LICENSE / TAMPERED（record-only） | `core/AuthManager.java` |
| 诱饵（蜜罐） | `PromoConfig`(明文/base64) + `CompatProbe` 绊线（比 canary → markTampered）；**isVipAuthorized 非诱饵** | `core/PromoConfig` / `core/CompatProbe` |

## ④ 判据表（接手先看这栏：✅ 已装机 / 🟡 真缺口 / 🔵 意料内取舍）

> **🔵 = 有意取舍，别当债、别报缺口；🟡 = 真该补。** 判据/证据指针在第 3 列——代码即真相，与本表冲突以代码为准。

| 项 | 判定 | 判据 / 证据指针 |
|---|---|---|
| 授权最小闭环（卡密→token→envelope→AuthGate），客户端 pv 见发版真源 §③ | ✅ 装机 PASS（2026-06-11） | §10.6；本页未亲跑动态验证 |
| S4 Ed25519 验签（客户端只放公钥，拒 HS256/无签名） | ✅ 装机 PASS | `net/AuthEnvelopeVerifier.java`；线上 `alg=Ed25519` |
| S3b LeaseClock 服务器授时 + 断网>72h 设置页强验/失败撤销 | ✅ 装机 PASS | `core/LeaseClock` + `net/GuardHeartbeat` + `moduleB/SettingsEntry` |
| S3a `android_8071` prod_server_lock（无 server seed → registry 散沙） | ✅ 装机 PASS | `core/EncryptedConfigLoader`（summary=scatter fail-closed） |
| 应急停用三档（RELEASE_KILLED / VERSION_KILLED / cardRevoked） | ✅ 等价 PASS；**设备封禁(DEVICE_BANNED) 真机 e2e L1 已证 2026-07-03** | 见 §⑥ |
| 封停/删卡撤销（rf · D-020）：`isAuthorizedNow` 首关查 `isCardRevoked` | ✅ 已落码 | `net/EnvelopeStore.java` |
| 杂项功能 `isTamperDegraded()` 确认篡改后散沙 | ✅ 已落码（消费者以 `isTamperDegraded()` 调用点为准，grep 即得） | `core/RiskState.java` |
| 负向验收：人为写 `tk/bl/le` / 超长 lease 应失效 | 🟡 真缺口 | 重验逻辑在（`EnvelopeStore.getVerifiedCachedEnvelope` 每次重跑 Ed25519+device），但**无 L1 装机日志坐实**（2026-07-03 封禁 e2e 侧证了 fail-closed = 无有效授权→密友暴露；合成 `tk/bl/le` 注入测仍未做） |
| `EncryptedConfigLoader` 直接接 RiskState/LeaseClock（过期/篡改→直接 scatter） | 🟡 真缺口 | `core/EncryptedConfigLoader` 现只读 SO summary；类头自注 “LeaseClock/RiskState scatter-on-tamper is still TODO” |
| V3 官替/共存证书源 + 包名注入 + 每发行线回归 | 🟡 真缺口 | 须与 V3 发版流程绑定，未接通（§10.6 冲突警告） |
| 备节点 `miyou.lol` HTTPS 反代 | 🟡 真缺口 | 客户端当前只启用主节点 |
| 删 Filter 剩余内联明文（SearchFilter 等 5 处） | 🔵 意料内取舍 | **粗粒度取舍非缺口，判据唯一真源 `PROTECTION_MAP §10.5`**；删除动作延后并入 v2 |
| 官方时间当**主时钟源**替代自有心跳 | 🔵 意料内取舍 | 仅 KPI 收益、低价值、可不做（自标 §10.6 步6）；官方对时「第二源」防改/防冻已落地（§③） |

## ⑤ 服务器

miyou-server（外部 `I:\miyou-server`）：主节点 `zxmqq.shop` 已部署 activate / envelope / 后台卡密 / 设备封停 / 定向更新；备节点 `miyou.lol` HTTPS 待办。

应急三档（§10.2 减法收口）：整线停用 `RELEASE_KILLED` · 单卡封停 `cardRevoked=1` · 暂停新激活 `RELEASE_PAUSED`；**无独立 killSwitch 字段**（本地 stub 已删）。

## ⑥ 应急停用三档（2026-07-01 路A + 版本停用 · 等价 PASS）

用户拍板走路A（整线停用立即生效）+ 加版本停用。客户端已改 `GuardHeartbeat.isHardAuthError` → 收到下列码即清 token → 下次心跳/冷启动撤授权（不再靠缓存自然过期）。

| 档 | 错误码 | 单位 | 状态 |
|---|---|---|---|
| 整线停用 | `RELEASE_KILLED` | release_id（官替/共存全部版本） | ✅ 等价 PASS（客户端码 audit + 服务器 API 侧同 killed 判定链已实证） |
| 单版本停用 | `VERSION_KILLED` | (release_id, pv) 精确到某版本 | ✅ 等价 PASS（客户端码 audit + 服务器 API 端 11/11 实测 v0.0test 命中并回 403） |
| 单卡/设备停 | `cardRevoked=1` / `CARD_*` / `DEVICE_BANNED` | 卡 / 设备 | ✅ 既有 + **设备封禁真机 e2e L1（2026-07-03）**，详见下 |

- 设计稿：`03_execute_执行任务/P_VersionKill_版本停用/设计稿_版本停用VersionKill.md`
- 等价 PASS 说明：F.4 主线链路以「客户端 auth-review + 服务器 API 端实测」双证收口；真机 LSPatched-Release 装机 L1 亦已 PASS（2026-07-01 官替 + 共存双线 `recipeOk=true`）。
- 文档已同步：`PROTECTION_MAP §10.6:272` 已由 5 码更新为 7 码（+ RELEASE_KILLED / VERSION_KILLED）。
- **设备封禁真机 e2e L1（2026-07-03 · P91 · 小米9 609b4b18 官替）**：后台封禁 → `[hb] hard auth error → token cleared` + `result=DEVICE_BANNED` + `envelope invalid → fail-closed` → `isAuthorizedNow=false` → **密友重现（UI 实证功能失效）**；`[A2SIG] installed` 未连坐（A2 不依赖 token）。解封 + 重激活 → `[act] token acquired` → `recipeOk=true` + `result=OK` → **密友重藏（UI 实证）**。日志 `logs/p91_ban_test.txt` + `p91_recover.txt` + UI 双证。⚠️ 本次走 **DEVICE_BANNED 硬错误码路径**（清 token、需重激活恢复）；`rf=1` 卡撤销路径（`markCardRevoked`，D-020 连坐 A2+隐私、72h A2 grace、有效信封自动恢复不需重激活）仍未单独 e2e 验。

## ⑦ ⚠️ 别再信的旧说法

- `PROTECTION_MAP §2 基线表`：「isVipAuthorized(){return true} 假锁」→ 已被 §10.6 更正。
