# 防封 · 当前真源（Guard Native）

> 单一用途：**碰防封 / 反检测时只读这一页**。要改防封代码 → 先过 `防封官(guard-antiban)` + `授权检查官`。
> 状态：2026-07-01 立（各轴经用户核对）。现状浓缩；细节深链、不复制（G10）。真相基准 = G8 代码即真相。
> 深链（要挖细节才点）：机制 `03_execute_执行任务/P_AntiBanGate_防封授权闸/DESIGN.md` · 热更新闸/HUF `03_execute_执行任务/P_HotUpdateFreeze_官方热更新冻结/brief.md` · A2 唯一真源 `…/SSOT_A2授权防破解_统一真源.md` · 时间/配方数值 `…/配方卡_SPEC_v1.md` · 场景表 `…/授权风险场景矩阵_SPEC.md` · KPI/进程白名单/上报 `.cursor/skills/guard-antiban_防封官/SKILL.md` · 命脉账 `C:\Users\Me\Desktop\防封_反检测线\防封权威账_2026年6月.md` · 决策 `DECISION_LOG.md D-018/D-019/D-020/D-021`

## ① 大白话

- 防封不是"造检测"，是**中间程序骑在官方查身份的咽喉上**（`getPackageInfo` → native `c$p`），**灌官方值让我方数据读出来像「官方、正常」**（签名 / 包名），尽量不被判异常。
- **只做一件事 = 让数据正常**（尽力、**非保证**）。官方最终怎么判、加密直发它服务器，**我方读不到、不预测、不保证保号**。
- **顺手借官方的眼睛**读输入变没变（签名被换 / 包被改）= 抓改包破解 → 散沙 / 影子期。
- **零环境读取**：`ro.boot.*` 官方自己读，我方零读（守 KPI；第三方读 verifiedbootstate 自伤）。root 不硬查、付费即正版。

## ② A2 两轴（官替 vs 共存 · 用户 2026-07-01 核对 · 真源 SSOT §1.1）

| 轴 | 官替版 | 共存版 | 为什么 |
|---|:--:|:--:|---|
| **签名** | 要喂 | 要喂 | 两形态都重签、签名偏离官方 DER（硬轴，L1 已证）|
| **包名 / 路径** | 通常不需要 | **要喂** | 官替已占官方包名 `com.tencent.mm`；共存 `com.tencent.mn` 不同，需在检测 caller（`normsg`/`bu5`/`c$p`）处**选择性**灌官方包名，禁全局改 `getPackageName` |

- 一句话：**官替 = 签名；共存 = 再加包名。**
- **共存包名铁律**（详 SSOT §1.1）：禁全替 `.mm`→`.mn`，只在官方检测咽喉灌官方包名/路径/DER；签名须同覆盖 `signatures[]`+`signingInfo`（漏一条即穿）。

## ③ 三闸（各管一摊、互不连坐）

| 闸 | 出口（代码） | 失败方向 | 为什么这个方向 |
|---|---|---|---|
| **A2 防封闸** | `AntiBanGate.isAntiBanReady()` | **fail-OPEN**（拿不准就装）| 误判 = 不再保号（不可逆），宁错放勿错杀 |
| **隐私闸** | `StateMachine.isActive()` 四层 AND | **fail-CLOSED**（拿不准就散）| 误判可逆（重授权即回）+ 变现门，宁错关 |
| **热更新闸** | `HotUpdateFreeze`（`AppConfig.isHotFreezeEnabled` 默认 true）| **默认 FREEZE**（冻结）| 冻官方 Tinker/整包更新锁版本，防热补丁换掉重打包 / 改 hook 依赖类（详 DESIGN/HUF）|

- 真防护边界 = `isSensitiveConfigReady`（无合法 envelope，`getRecipe()` 出不来真 registry）。hook `isActive`/`isVipAuthorized` 改 true **没用**；红队唯一破防标准 = 无 envelope 仍让 `getRecipe()` 出真 registry。
- **签名读点隔离（防破解命门 · 别碰）**：A2 灌值只 hook `getPackageInfo`（读已装包→喂官方给检测大血管看）；防破解 `CompatProbe.certOfModule`（抓重签）+ `ModuleMain.bindSigningCert`（算 registry key）走 **`getPackageArchiveInfo`（读 APK 文件）读真实签名**。两 API 零交集 → A2 灌官方不污染防破解。**禁给 A2 加 `getPackageArchiveInfo`/`getInstalledPackages` hook**（灌了→盗版重签也算出真 key→散沙失效、反白嫖破功；且探针实测这俩检测零命中，加=纯负收益）。L1：2026-07-08b root 真机（worklog 2026-07-08b 节）。

## ④ 撤闸场景（口径 · 逐场景全表唯一权威 = 场景矩阵 SPEC §3 · D-020/D-021）

- **A2 保号**：付费中 / 断网短 → 装；自然到期 → 7 天后撤；封停 / 删卡 / 首装未授权 → 72h 后撤（官方授时起算·我方零上报）；**重签 / 篡改 → 立刻散（零宽限·不可逆）**。
- **隐私**：付费 / 断网 → 开；到期 → 立刻关；封停 / 篡改 → 立刻散。
- **可信时间命门**：服务器授时 + `elapsedRealtime` 单调 + 官方对时第二源 `jy0.hd.b()`（改表杀不掉·L1 已验）；超 2 年 = 篡改影子。拿不到 = fail-open 装。

## ⑤ 代码现状（code-true · L2 待 L1，锚点详 SSOT §8）

| 件 | 类 / 方法 | 现状 |
|---|---|---|
| A2 防封闸 | `core/AntiBanGate.isAntiBanReady` | ✅ cert 完整性 + 时间闸（72h/7天 fail-open）·L2 待 L1 |
| A2 喂官方（签名轴）| `core/A2SignatureSpoof`（`OFFICIAL_DER_HEX`）| ✅ live，reboot `[A2SIG] installed`；由 `ModuleMain §6.7` 在闸 true 时装。**2026-07-08 减法收口 2 入口** `getPackageInfo(String,int)`+`(String,PackageInfoFlags)`(A13+)；A11 三轮栈追踪 L1 证 c$p/t8/bu5/oy5 读自身签名唯一源 = getPackageInfo(String,int)、AsUser 仅 framework 内部转调、Archive/枚举零命中 → 老 hook 已覆盖全部下游；不挂 AsUser(冗余增暴露)·**2026-07-08e A15/A13+ L1 闭合**（`installed sig hooks=2/2` + `fed via NEW-overload` 微信真走新重载灌官方；A16 同架构高置信）|
| 包名轴（共存）| `A2PkgPathSpoof`（`ModuleMain` 接线，仅共存生效）| ✅ 已落码接主线；2026-07-08b root 真机灌值坐实；**2026-07-08f/g 拓 A13+ 新重载 4 入口 + A15 L1 闭合**：`[A2PKG] installed hooks=4/4`（A11=2/4）+ `fed getPackageInfo/getApplicationInfo via NEW-overload pkg=com.tencent.mm` 真走新重载灌官方包名+路径 |
| 可信时间 | `core/LeaseClock` + `core/OfficialClock` | ✅ 服务器授时 + 官方对时第二源已接·L2 待 L1 |
| 篡改散沙 | `core/RiskState.isTamperDegraded` | 消费者以 `isTamperDegraded()` 调用点为准（grep 即得，别抄死数；**密友四链不进散沙**，铁律 29 防误伤正版）|

## ⑥ KPI 红线（出包前体检 · 数值唯一权威 = 防封官 skill / CLAUDE §七）

- 四红线 verifiedbootstate / normsg / PROP / CONN —— **具体数值详 skill / CLAUDE §七，勿在此复制**。
- **LSPosed 进程白名单**：只 `com.tencent.mm` 主 + `:push`（最小拦截）；❌ 永久禁 `:sandboxed_process`/`:isolated_*`/`:appbrand*`。
- 环境类（vbs/PROP）我方零读取本就达标；密度类（normsg/CONN）明显异常才查。工具 `frida_stats.js`。

## ⑦ 已装机 ✅ vs 待做 🟡

**✅ L1 已证**：A2 能抓能骑（2026-06-25 `recon/A2_RIDE_TEST`）· 首装未授权 A2 装机 PASS（2026-06-26 E99）· **热更新闸 FREEZE 实拦查更**（2026-07-01 官替 `[HUF] p53.j.b→blocked`；共存 A2SIG/A2PKG/HUF installed + 隐藏删密友）· 蜜罐绊线 / 引流弹窗 / 不误伤正版 · **签名轴灌值闭环**（2026-07-08b root 真机 warm-attach：共存包 mn `getPackageInfo` 咽喉进 `cdc5decb` 非官方底签名 / 出 `18c867f0` 官方，c$p.ad/aa/af + t8 + oy5 全读官方进 toProtoBuf、检测链零残留；AsUser 的 cdc5decb 是 hook 内 invokeOriginalMethod 中间态非泄漏）
- **⚠️ 扫脸 = 官替可用 / 共存不可用（native 硬命门·出货定性）**（2026-07-08c/d L1 真机对照）：**官替版 `com.tencent.mm` 扫脸摄像头能展开=通过**；**共存版 `com.tencent.mn` 直接 `init lib failed`、连摄像头都开不了=不可用**（链条级损坏、死在 VA scan 之前；表现不稳定：有时到 `FaceProNative.checkByNative`=1 报系统繁忙、有时更早 init lib failed）。VA scan 走 native 读包名、不走 `getPackageInfo` → A2 现有轴覆盖不到。**「native 也加」已否决**：共存扫脸死在 checkByNative 之前，加 native 处理它救不了 + 碰微信 native 违铁律2/23 + Matrix 监控 native hook 反送封号信号。⇒ **要扫脸必用官替版（零成本天然满足）；不加 native、不新增 A2 轴（做减法）**。详 worklog 2026-07-08c/d + `tools/dump_vascan.js`/`dump_vascan_patch.js`
- **热更新时间线**：`AppConfig` 环形 10 条本机记录、只记真事件（`m53.d0.j`/`m53.d0.d`/`fl4.o.Bg`）、零上报、adb 采；`hul` 落第一条待官方真推补丁。
- **✅ 签名轴 A13+/A16 新重载 L1 闭合**（2026-07-08e vivo A15 无 root 官替装机）：A15 上 `[A2SIG] installed sig hooks=2/2`（A11=1/2 → A13+ 新重载 `getPackageInfo(String,PackageInfoFlags)` 真实存在且 hook 挂上）+ 登录触发 c$p 后 `fed OFFICIAL via NEW-overload(PackageInfoFlags)` **和** `via OLD-overload` 都命中 flags=0x40 → **微信 A13+ 新老重载都走、都灌官方成功**。减法收口时加的第二入口 = A13+ 必需（非冗余）。签名轴 A11(07-08b)+A15/A13+(本轮) 双版本 L1 证实；A16 同 A13+ 架构高置信覆盖。「签名是否 A16 封因」仍 L4（禁猜）。详 worklog 2026-07-08e。

**🟡 待 L1 / 待做**：A2 时间闸 8 分支自测（07-08e A15 装机 `[ANTIBAN-BRANCH]` 全 PASS ✅）· 官方对时接线（L2 待 L1）· Batch3 删全局 W（门禁 D-029 已简化为服务器单方判定）· canary② 绑行为 · 删 Filter 明文 fallback（绑 V3 发版）· 「等 7 天→散沙」时间链现场验 · 详 `…/P_AntiBanGate_防封授权闸/worklog.md` 2026-07-08g/08f/08e/08d/08b/07-05 节 + `recon/so_extract_20260705/`

**✅ A2 身份轴 A13+/A16 全闭合**（2026-07-08g A15 真机 L1）：签名（signatures[] 0x40 + signingInfo 0x8000000）+ 包名（packageName）+ 路径（sourceDir）三轴的 A13+ 新重载（getPackageInfo/getApplicationInfo × PackageInfoFlags/ApplicationInfoFlags）**全部 A15 真机实测走到并灌官方**（`installed sig hooks=2/2`+`A2PKG hooks=4/4`+`fed via NEW-overload`）；A16 同 A13+ 架构高置信覆盖。「签名/身份是否 A16 封因」仍 L4（禁猜）。

## ⑧ ⚠️ 别再信的旧说法

- ~~D-018「未授权永不撤 / 免费」~~ → **D-020 取代**：首装 72h、到期 7 天、封停/删卡 72h、可恢复（收有效信封清 latch）。
- ~~「A2 不存在 / 主线零 A2 代码 / isAntiBanReady 需新建」~~ → **已 live**：`A2SignatureSpoof` + `isAntiBanReady` 存在、reboot `[A2SIG] installed`（SSOT §6 定死）。
- 「退款」概念 → **无**；撤销唯一动作 = 服务器**封停 / 删卡密**（符号 `isCardRevoked`）。
