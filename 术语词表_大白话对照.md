# 术语词表 · 大白话 ↔ 技术锚点对照

> **干嘛用的**：给非技术的项目主看的「叫法索引」——一个概念，左边是你听得懂的大白话代号，右边是代码/文档里的技术锚点。以后所有文档先用大白话讲、再跟技术锚点。
>
> **三条维护守则（钉死）**：
> 1. **这是索引、不是真源**：每个机制的「权威定义」仍在各自文档（见「真源」列）；本表只做「叫法 ↔ 锚点」映射，**不重写机制**（守 G10 单一真源，防两份漂移）。
> 2. **代号只进文档 / 口头，不进 release 代码**：`顺风耳`/`eavesdrop`/`保险柜` 这类明词写进 release 码 = 给逆向者指路。代码里保持中性标识符（`re` / `derive_registry_key`），R8 会混淆。
> 3. **一个概念只一个代号**：别一会「顺风耳」一会别的（防漂移）。新增代号先查本表有没有。
>
> **命名规约**：照 `CLAUDE.md` 定死词表——写「官方包 / 原版」不写品牌名；写「封 / 账号异常」不写「封号」全词。

---

## 一、加密 · 钥匙 · 防破解

| 大白话代号 | 技术锚点 | 一句话 | 真源 |
|---|---|---|---|
| **保险柜** | 加密 registry（hook 配方表）+ `libguardcore.so` | 锁着「哪些 hook、怎么藏密友」的真配方 | `PROTECTION_MAP §3-5` |
| **4 颗钥匙牙（与门）** | `derive_registry_key` 折入的 4 种料 | 4 颗全咬合才解得开保险柜，缺一即散沙 | `钥匙加固_KeyHardening设计 §0` |
| **牙① 身份证** | 模块签名证书 SHA-256（`g_binding`） | 重打包重签 → 钥匙错 → 散沙 | KeyHardening §0 |
| **牙② 服务器种子** | `S_rel` / `prod_server_lock`（`GUARD_REGISTRY_REQUIRES_SERVER_SEED=1`） | 没服务器种子 → 解不出（呼不走的锁的根） | `PROTECTION_MAP §10.6` |
| **牙③ 一机一密** | `W_dev = KDF(wseg_a/b/c + 设备材料 dm)` | 每台机钥匙不同，抽一台 SO 不能通杀别的机 | KeyHardening §2 |
| **牙④ 防重放 / 过期（a 案）** | `unwrap` 后比 `expire_at <= trusted_now`（官方授时 floor） | 旧 / 过期信封重放 → 散沙；**不折静态 key**（折了正版续约会自锁） | KeyHardening §3 |
| **老万能钥匙** | 全局 W（`g_wk_lo` / `g_wk_hi`，静态明文一把） | 一把通用、抽一台通杀；待 Batch3「收口」删掉 | KeyHardening §5 |
| **散沙** | fail-closed scatter（GCM 解不开 / 无种子 / 过期） | 解不开就吐乱码——**不崩、不全开、不删数据、不毁原版** | 安全官 skill |
| **通行票 / 续票** | 信封 `envelope` / `expire_at` 续约 | 服务器签发的授权凭证，会过期、要续 | `PROTECTION_MAP §10.6` |
| **掐咽喉、灌官方值** | hook `getPackageInfo` / `c$p`，喂官方签名 DER | 官方包自检读到熟料（官方态）→ 数据读出来尽量是官方正常态 = **保号** | `A2SignatureSpoof` / `P_AntiBanGate/DESIGN` |
| **顺风耳** | `A2SignatureSpoof.observeBorrowed()` → `getBorrowedEnvSignal()` → `EnvelopeClient` 发 `re` | 借官方的眼睛被动看到 root/RE 弱信号，把听到的捎给**自家服务器**做弱权重 | `A2SignatureSpoof` / `P_AntiBanGate/DESIGN` |
| **借官方的眼睛** | 同「顺风耳」——被动观测，零主动枚举、零环境读取 | 不自己扫手机，只瞄官方包查到的（守红线 #3/#5） | `CLAUDE.md` 核心打法 |

> **「保号 vs 抓破解」一句话**：掐咽喉**灌官方值** = 让官方包查我方签名时读到官方 → 保号；同一个掐点**借官方的眼睛**读「签名变没变」= 抓改包破解者。

---

## 二、授权 · 风险 · 引流

| 大白话代号 | 技术锚点 | 一句话 | 真源 |
|---|---|---|---|
| **真授权门** | `StateMachine.isVipAuthorized()` = `EnvelopeStore.isAuthorizedNow()` | 真判有没有授权（token + Ed25519 验签信封 + license 未过期），不是 `return true` 假锁 | `PROTECTION_MAP §10.6` |
| **中央总闸** | `StateMachine.isActive()`（含 `isSensitiveConfigReady()`） | 所有过滤器都走它，改一处全生效 | `PROTECTION_MAP §1/§10.7` |
| **影子期** | `RiskState.SHADOW_HOURS_DEFAULT`（当前 168h） | 篡改后先不发作、过一段才散沙——让破解者定位不到因果 | `PROTECTION_MAP §10.9` |
| **蜜罐诱饵 + 绊线** | `PromoConfig`（明文诱饵）+ `CompatProbe`（canary / 签名校验） | 假引流配置给关键词党上钩；被改 / 重签 → `markTampered` | `PROTECTION_MAP §5/§10.9` |
| **引流弹窗** | `RiskPromptController` / `FunnelPrompt` | 确认盗版 / 超宽限才弹引流（唯一弹窗策略源） | `PROTECTION_MAP §10.8/§10.9` |
| **停用闸** | `kill_switch`（发行线级） | 服务器一键停某个版本（优先级最高） | `PROTECTION_MAP §10.2` |
| **封停/删卡撤销** | 信封 `rf=1`（`rf` 仅 wire/存储键的历史缩写，**勿读作退款**）→ 客户端 `isCardRevoked` latch → 散沙 | 撤销只有三种：**到期 / 后台封停 / 删卡**（**本项目无退款概念**）；服务器发 `rf` 位 → 客户端撤 A2+隐私 | `SSOT §3/§8` |

---

## 三、产品形态 · 命名规约（引 `CLAUDE.md` 定死词表）

| 大白话 / 正确叫法 | 别写 | 说明 |
|---|---|---|
| **官方包 / 原版 / 官方客户端** | 品牌名 | 概念叙述一律这么写 |
| **重新打包**（用 LSPatch 把模块打进原版） | 「注入」（指产品形态时） | 例外：禁止的「底层注入三件套」是技术正确词，照写 |
| **宿主 APK / 宿主包名** | —（打包线术语保留） | 被重新打包进去的那个包（可官替原版、也可共存克隆） |
| **封 / 账号异常** | 「封号」全词 | — |
| **灌官方值 / 喂官方值** | spoof / 伪造 / 欺骗 | 往官方检测回答官方真值 |
| **防破解 / 逆向 / 反编译** | （直接写，不回避） | 我们是正派一方、护自家产品 |

---

## 四、灰度 / 批次（钥匙加固专用）

| 大白话 | 技术锚点 | 状态 |
|---|---|---|
| **打地基** | Batch 0（`derive_wrap_key` + 向量 + 空接 JNI，不碰 live） | ✅ 已落 `caf2142` |
| **铺路双试** | Batch 1（`unwrap` 双试：先 W_dev、失败回退老万能钥匙） | ✅ 已落 `caf2142` |
| **按设备切** | Batch 2（服务器按 `dm` 逐台发 W_dev 信封） | 🟡 本地写完、未部署 |
| **收口锁定** | Batch 3（删老万能钥匙 + 启用牙④ expire 检查） | ⬜ 冻结待 Batch 2（施工卡已备） |

---

## 五、内部代号速查（决策 / 失败 / 会话 / 钥匙编号）

> ⚠️ **这一组最晦涩**——纯编号，不翻日志根本猜不到。人话列用真名（微信 / 蜜罐 / 破解）方便看懂；**正文 / 代码 / 出货文档仍守 §三「官方包 / 不写品牌名」**（本节只是解码索引）。

| 代号 | 人话 | 真源 |
|---|---|---|
| **D-018 / D-019 / D-020 / D-021** | 「拍板决定」第几号（D-019 篡改不可逆 / D-020 加时间闸 / D-021 封停删卡 72h） | `DECISION_LOG.md` |
| **A3** | D-020 之后的补丁拍板（封停删卡=72h、仅自然到期=7天） | `配方卡_SPEC §A` |
| **G10 / G88 / G92** | 全局铁律或某轮对话号（G10「一结论只写一处」/ G88 用户拍板那轮 / G92 官方授时证据） | `CLAUDE.md` / worklog |
| **F-22 / F-23 / F-31 …** | 「踩过的坑」第几号（F-22 KPI 没建 / F-23 注入微信崩 / F-31 动了已验证 hook 静默失效） | `FAILURE_LOG.md` |
| **C18 / C36 / A39 / A51** | 演进史里「哪次会议定的」节点号 | `DESIGN 附录E` |
| **S0–S9** | 实施步骤第 0~9 步 | `配方卡 §C` / `DESIGN §7` |
| **[码]** | 标记「这条已经写进代码了」 | — |
| **成色（L1/L2/L3/L4/设计only）** | 这条结论多可信：L1 跑出来看到 / L2 代码里确认 / L3 推断 / L4 还没验 / 设计only 只是设想 | `CLAUDE.md §三.五` |

## 六、微信侧锚点 + 比喻补全

> 人话用真名「微信」（解码用）；边界同 §五。

| 黑话 / 锚点 | 人话 | 真源 |
|---|---|---|
| **A2（防封线）** | 防封的「灌官方值保号」总能力 = 三轴（签名 / android_id / 包名）喂官方，受 isAntiBanReady 门控 | `DESIGN §4` |
| **A2SignatureSpoof / A2PkgPathSpoof** | 喂官方签名 / 喂官方包名 的两个模块 | `ModuleMain §6.7` |
| **⚠️「A2」撞名** | 防封线 A2 = 喂官方值保号；**隐私 hook 线 A2 = 密友导入**（`CLAUDE.md` v1 hook 列表）——两个 A2 不同事，看文档先认是哪条线 | `CLAUDE.md` |
| **c$p** | 微信里查身份（签名 / 包名 / android_id）的那个类 = 咽喉 | `DESIGN §0/§4.1` |
| **hd.b() / jy0.hd.b()** | 微信自己的对时函数（改手机时间也骗不动）= 地板值 | `DESIGN §6` |
| **大血管** | 数据上传的主通道（如 android_id 走 `c$p.aa/ea`） | `DESIGN §4.1` |
| **隐藏血管** | 别的隐蔽数据暗道（「签名轴无隐藏血管」= 没暗道） | `DESIGN 附录B` |
| **命门 / 命脉** | 最关键、不能被冻结的点（如可信时间） | `DESIGN §6` |
| **生料 → 熟料** | 生料 = 没加工的真实数据（改装包本来的签名）；熟料 = 加工成官方样子、喂出去的料 | `DESIGN §0` |
| **水位（只抬不降）/ 对时** | 可信时间只能往上走、防回拨 / 跟官方校时 | `DESIGN §6` |
| **三轴** | 防封要喂官方的三个点：签名 / android_id / 包名 | `DESIGN §4` |
| **签名轴** | 三轴之一：喂官方签名 DER（`18c867f0`）；**非 root 可喂、目前唯一稳的硬轴** | `DESIGN §4.1` |
| **android_id 轴** | 三轴之一：喂「本机官方签名对应的 SSAID」（每台机不同，root 实验机算一次入库） | `DESIGN §4.1` |
| **包名 / 路径轴** | 三轴之一：喂官方包名 / 路径；**主要共存版用**（官替已占官方包名通常不需要） | `DESIGN §4` |
| **root / 解锁轴** | ❌ **不做**：借不到官方现成判定 + 读环境踩红线；只当服务器侧软信号 | `DESIGN §4.1` |
| **热更新 / Tinker 热补丁** | 微信自带的运行时打补丁：UI/插件类由 `DelegateLastClassLoader` 加载；**hook 这类类必须用 `app.getClassLoader()`**（用 base.apk 的钩不上） | `ModuleMain` / 铁律 |
| **类加载器（加载）** | `lpparam.classLoader` = base.apk 的；`app.getClassLoader()` = 含热补丁的运行时——hook 热更新类用后者 | `ModuleMain` |
| **root（设备 root）⚠️撞名** | 指**已 root 的手机**（≠「类加载」）；本项目把 root 当**服务器侧软信号**、客户端不查不本地触发（开发机 root 一月零封，root 非封因） | `DESIGN §0` / 铁律5 |
| **官替 / 共存** | 占微信原包名的版本 / 改了包名跟微信并存的版本 | `RELEASE_RULES` |
| **三态：正版 / 观望 / 蜜罐** | 付费没篡改 / 装了没付费（白嫖）/ root·篡改·长期没通信 | `DESIGN §1.1` |
| **保号** | 让微信号不被判异常 | `DESIGN §0` |
| **防封闸 isAntiBanReady** | 「要不要装防封」的开关 | `GuardRuntime.java` |
| **时间闸 evalAntiBanWindow** | 算「什么时候撤防封」（首装 72h / 到期 7 天） | `GuardRuntime.java` |

## 七、最难懂 TOP（优先去黑话清单）

> 你问的「太晦涩的」就这些——**不翻日志 / 不逆向根本猜不到**，建议文档里首次出现都挂本表链接：

1. **纯编号**：`D-020` / `A3` / `G88` / `F-31` / `C36` / `S_rel` / `W_dev` / `[码]` / `成色`
2. **微信混淆名**：`c$p` / `hd.b()` / `na4.b` / `MvvmList.n` / `kc5.v0`
3. **没上下文猜不到的比喻**：`大血管` / `隐藏血管` / `命门` / `生料·熟料` / `水位` / `地板值`

（相对好懂、连蒙带猜还行的：喂 / 料 / 咽喉 / 蜜罐 / 保号 / 散沙 / 影子期 / 白嫖 / 官替·共存。）

---

*本表 = 叫法索引（单一真源是各自「真源」列文档）。新增代号先查本表、一概念一代号、代号不进 release 代码。*
