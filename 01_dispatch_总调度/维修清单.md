# 维修清单 — 待修 Bug 跟踪

> 维护人：guard-dispatch_总调度
> 用途：登记装机/使用中发现的待修问题。证据级标注；用户口述先记 🟡 待复现，复现+定位后转可修。
> 与 `FAILURE_LOG.md` 区别：FAILURE_LOG 记"已证伪的方案"；本表记"待修的功能缺陷"。

---

## V3 免 root 改包（VIVO / Android 15，2026-06-08 用户装机口述）

> 来源：用户在 LSPatch 改包微信 8.0.71 上实测口述（🟡 待复现 + 待 logcat 定位，G6：未抓到日志前不作 L1 结论）。

| # | 现象 | 初判方向（待验证） | 状态 |
|:--:|------|----------------|:--:|
| R-001 | **桌面图标角标没隐藏**：密友发消息时，launcher 桌面图标上的未读角标/红点仍显示出该消息 | PushFilter / badge 守护在改包环境可能没生效：① `:push` 进程在 LSPatch 改包里是否被注入存疑；② VIVO launcher 的角标 provider 链路可能与已验机型不同。需抓 `:push` + badge 写入链 logcat 定位 | 🟡 待复现+定位 |
| R-002 | **密友设置页左滑返回无反应**：在密友设置 overlay 页，安卓系统边缘左滑手势不能返回，必须点左上角按钮 | SettingsEntry overlay/容器未消费系统返回手势（onBackInvoked / 边缘手势 dispatch）。属 UI 交互缺陷，不影响功能 | 🟡 待修（低优先） |

### 复现/定位待办
- R-001：adb 稳定后，密友发消息 → 抓 `adb logcat -s NCL` 看 `:push` 是否注入、badge 拦截是否命中；确认 VIVO launcher 角标走哪条链。
- R-002：定位 SettingsEntry overlay 的 window/dialog 类型，补系统返回（OnBackPressedDispatcher / KEYCODE_BACK / 边缘手势回调）。

---

## 维护约定
- 修复后：状态转 ✅ + 注明提交号 + 装机日志路径；保留历史行不删。
- 升级为正式 P 任务时：在此登记 P 号回链。
