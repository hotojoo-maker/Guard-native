# W1 交接快照 — 2026-05-19

## 当前状态

| 任务 | 状态 | 说明 |
|------|:----:|------|
| P15 脚手架 | ✅ | KPI全绿（verifiedbootstate=4），所有调试工具正常 |
| P16 朋友圈 | 🟡 | v12 方案重写中，执行 agent 正在写 MomentsFilter.java |
| P17 会话 | ⬜ | 等 P16 闭环后开 |
| P18 离线采集 | ⬜ | 有在线测试，优先级降低 |

## 今日关键决策

1. **parseFrom 方案废弃（F-27）**：8.0.66 走 JNI/C++，Java hook 零命中。CLAUDE.md §5.2 已改写。
2. **P16 改用 filter_moments.js v12**：MvvmList + ArrayList 实例拦截，L1 验证。
3. **3 角色工作流**：总调度 / 执行 / 审核（含卡住自动停规则）。

## 今日新增铁律（F-23 ~ F-27）

| # | 铁律摘要 |
|---|---------|
| F-23 | 禁止 JniHook/JNI_OnLoad → 微信强制下线 |
| F-24 | 禁止 extends Service → Service not found |
| F-25 | XposedHelpers 必须 catch Throwable → init 静默中断 |
| F-26 | hook protobuf 用 getMethods() + XposedBridge.hookMethod |
| F-27 | 禁止 SnsObject.parseFrom → 8.0.66 JNI/C++，零命中 |

## P16 下一步（执行 agent 正在做）

- 重写 MomentsFilter.java（v12 方案）
- 装机验收：加 wxid → 刷朋友圈 → 看不到 → F05 计数 +1
- frida_stats KPI 对比 P15 基线

## 8.0.66 已确认字段（T05 产出）

| 字段 | 值 |
|------|---|
| 朋友圈 item 类 | `k24.b` |
| wxid 字段 | `Username` |
| Like/Comment item 类 | `gu4.ux5` |
| item wxid 字段 | `d` |

## 文件路径速查

```
src/main/java/com/ghost/assist/moduleD/MomentsFilter.java  ← P16 主文件
refs/filter_moments.js                                      ← v12 参考
docs/HOOK_POINTS.md §三                                     ← F05 hook 点
FAILURE_LOG.md                                              ← 27 条铁律
```
