# W2 交接快照 — 2026-05-19 19:27

## 当前状态

| 任务 | 状态 | 说明 |
|------|:----:|------|
| P15 脚手架 | ✅ | KPI 全绿，调试工具正常 |
| P16 朋友圈 | 🟡 | **拦截功能已通，UX 跳顶问题调优中** |
| P17 会话 | ⬜ | 等 P16 关闭 |
| P18 离线采集 | ⬜ | 待开 |

---

## P16 当前进度

**已验证通过**
- `ArrayList.addAll` hook 命中 k24.b 数据 ✅
- wxid 提取路径：`k24.b → d(i24.p) → field_userName` ✅
- F05 计数正常（x5→x53 累积）✅
- 隐藏列表 wxid 匹配后 blocked 日志出现 ✅

**正在调的问题**
- 快速下滑偶发跳回顶部（非必现）
- 当前方案：beforeHookedMethod + `it.remove()` 修改原始 collection（v12 Frida 同款）
- APK 已 build，Claude Code 正在装机测试最新版本

**已排除的方案**
- `MvvmList.m()` 类级别 hook → 零命中（F-28）
- `RecyclerView.setAdapter()` hook → WeChat RecyclerView 是混淆版，无法找到
- afterHookedMethod 从 ArrayList 删 → 调用方 DiffUtil 见到 size 不一致仍跳顶
- `param.args[0]` 替换新 collection → 调用方持有旧引用，size 不一致

---

## 新增铁律（今日）

| # | 铁律 |
|---|------|
| F-28 | 禁止 MvvmList.m()/s() 类级别 hook → 8.0.66 朋友圈数据不走 m()，正确路径 y1→H→o/p→addAll |

---

## 关键字段（今日确认）

| 项目 | 值 | 来源 |
|------|---|------|
| 朋友圈 item 类 | `k24.b` | v12 + 本次验证 |
| item.d 类型 | `i24.p` | 今日 DUMP |
| wxid 字段 | `field_userName`（在 i24.p 上）| 今日 DUMP |
| ⚠️ T05 的 `Username` | SnsObject 上的字段，**不是** k24.b 路径 | 区分清楚 |

---

## 下一步（接手后按顺序）

1. **等 Claude Code 装机结果**：看最新版（in-place remove）跳顶是否消失
2. 如果还跳：考虑 `LinkedList` 替代（WeChat collection 类型可能不支持 iterator.remove）
3. 跳顶问题解决后 → 跑 `frida_stats.js spawn` KPI 门控（F-22 铁律）
4. KPI 通过 → P16 ✅，开 P17 会话 LSPosed 翻译

---

## 文件速查

```
src/main/java/com/ghost/assist/moduleD/MomentsFilter.java  ← P16 主文件（当前版本）
refs/filter_moments.js                                      ← v12 参考（对照用）
FAILURE_LOG.md                                             ← 28 条铁律
build/outputs/apk/debug/guard-native-debug.apk             ← 最新 APK
```

## ⚠️ Cursor ↔ Claude Code 分工（必须遵守）

- **Cursor** = 写/改代码，文档归档
- **Claude Code** = 跑命令（adb / build / frida / logcat）
- 每次要 Claude Code 执行操作，**必须用可复制块**输出：

```
告诉 Claude Code：

[具体命令]
```

- ❌ 不给 Claude Code 改代码指令（它只跑命令）
- ❌ 不用散文描述命令（用户不知道复制哪段）

---

## 调试命令

```bash
# 装机
adb install -r "build/outputs/apk/debug/guard-native-debug.apk"

# 看拦截日志
adb logcat -d 2>&1 | grep "NCL" | grep "blocked" | head -10

# 调试页
adb forward tcp:8080 tcp:8080
# 浏览器 http://localhost:8080 → 加 wxid → 刷朋友圈
```
