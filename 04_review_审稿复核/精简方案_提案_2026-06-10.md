# 文档精简方案（提案）— 2026-06-10

> 性质：**提案，未动手**。等用户拍板第 7 节的 3 个决策点后再执行。
> 实测底数采集：2026-06-10（`Get-ChildItem -Recurse *.md` + `git check-ignore`）。
> 维护：guard-review_质检门控（资料功能）。

---

## 0. 一句话

全盘 215 份 `.md` 里，**真·项目文档只有 151 份**，另外 64 份是 `node_modules` 依赖说明（已被 `.gitignore` 忽略，不入库）。
目标：把“接手必读核心面”压到 **~30 份**，全量项目文档归并/归档到 **~90 份**，**不丢任何已验事实**。

---

## 1. 现状底数（实测）

- 全盘 `.md`：**215**
  - `node_modules` 依赖文档：**64**（`git check-ignore` 确认 `tools/frida_build/node_modules` 已忽略 → 纯本地噪声，只在文件搜索时碍眼）
  - 真·项目文档：**151**
- 分布（按顶层目录）：

| 目录 | md 数 | 备注 |
|------|:--:|------|
| `tools/` | 64 | 几乎全是 node_modules 依赖 README/CHANGELOG（已 ignore）|
| `03_execute_执行任务/` | 59 | 每个 P 任务的 worklog/brief/result，大量已 ✅ 可归档 |
| `docs/` | 27 | 8071 主车道 + archive/isolation |
| `04_review_审稿复核/` | 10 | 审稿/复核记录 |
| `.cursor/skills` + `.claude/skills` | 8 + 8 | **镜像双份**，脚本同步 |
| `refs/` / `native_core/` | 6 / 6 | 竞品参考 / so 模块地图 |
| `01_dispatch_总调度/` | 4 | CURRENT_PLAN / NEXT_STEP 等 |
| 根级散文档 | ~13 | CLAUDE / HOOKMAP / TASK_BOARD / FAILURE_LOG / PROTECTION_MAP / CHATGPT_项目全景手册 … |

- 已被 `.gitignore` 兜住（不算仓库膨胀）：`node_modules/`、`tools/frida_build/`、`*.apk`、`build_*.log`、`bug排查/`、`jadx_8071_out/`、`dex_out/`、`DEV_SECRETS.md`。

---

## 2. 关键发现（不止“多”，有几处真问题）

1. **重复任务目录**：`03_execute_执行任务/` 下同时存在
   - `P17_会話LSPosed`（繁体「話」）
   - `P17_会话LSPosed`（简体「话」）

   同一任务两个目录，编码/手误造成。**必须合并**。
2. **看板与实际目录账实不符**：`TASK_BOARD.md` 列了 `P_NC1 / P_NF1–4 / P_CF2–5 / P_PF2` 等，`03_execute` 里没有对应目录；反过来 `03_execute` 有 `M6a / V3T5 / P20C / P26B / P_IMPORT` 等看板没单独列。
3. **三~四套编号并存**：
   - 主线：`P15–P26C`
   - native 子系：`P1A / P1B / P1C / P1E / P1F`（缺 P1D）
   - 语义号：`P_ConvWarm / P_CV1 / P_IMPORT`
   - 字母号：`E2 / M6a / T09 / V3T5`
4. **镜像 skill 双份**：`.cursor/skills/*/SKILL.md` 与 `.claude/skills/*/skill.md` 连**大小写都不统一**（SKILL vs skill），靠 `sync_skills.ps1` 同步，易脱节。

---

## 3. 分档处置（按风险从低到高）

### 档 A — 立即清理（零信息损失）
| 动作 | 对象 | 说明 |
|------|------|------|
| 合并重复目录 | `P17_会話…` → `P17_会话…` | 比对两边内容，保简体那份，繁体并入后删 |
| 本地噪声 | node_modules 64 份 | 已 ignore；删 `tools/frida_build/` 本地目录即可（需要时重生）|
| 清散日志 | `build_*.log`×5 等 | 已 ignore，删本地副本 |

### 档 B — 归档（可回溯，**move 不 delete**）
- ✅ 且 30 天以上的 P 任务 `worklog/brief` → `07_archive_归档/<P号>/`；根/execute 只留**一行结论 + 归档链接**。
- 候选：`P15 / P16 / P17 / P19 / P19B / P1A / P1B / P1C / P_CV1 / P_ConvWarm` 等已收口项。
- 预计可归 **~30–40 份**。

### 档 C — 合并（总览类去重）
- `CHATGPT_项目全景手册.md`(27KB) 与 `CLAUDE.md` / `PROJECT_INDEX.md` 高度重叠 → 收敛成 **CLAUDE（主入口）+ PROJECT_INDEX（路径）** 两份；全景手册降级为“对外简介”或并入。
- 其余“总览/决策”类按 git log 里已有的“文档收敛”口径继续合并到 1–2 份。

### 档 D — 护城河（**绝不动**）
`FAILURE_LOG`(F-01~F-38) · `docs/HOOK_MAP_8071_AUTHORITATIVE` · `docs/GUARD_GATE_TRUTH` · `PROTECTION_MAP` · `CLAUDE.md` 29 条铁律 · `PROJECT_INDEX` · `docs/CONV_REFRESH_PROBLEM`。

### 档 E — 编号统一（**先立规则 + 映射表，不强行重命名历史**）
- 新规则：主线只用 `P<数字>[字母]`；专题固定前缀（`NC`=native、`E`=取证/伪装、`T`=调研、`V3T`=v3 探针）。
- 历史目录**不改名**（避免断链），改为在 `PROJECT_INDEX` 加一张「**编号 ↔ 任务 ↔ 状态 ↔ 目录**」总表作为唯一权威；`TASK_BOARD` 只引用该表。
- 顺手修“账实不符”：看板里无目录的 `P_NF/P_CF/P_NC` 标真实状态或归并。

---

## 4. 执行顺序（每步一个 commit，安全优先）

1. **先 git 快照**：把当前 15 个脏文件分类提交，干净起步。
2. 档 A：合并重复 `P17` + 清本地噪声。
3. 档 E：先建“编号映射总表”（唯一权威），再据此修 `TASK_BOARD`。
4. 档 B：归档（move，不 delete）。
5. 档 C：合并总览（保留被并文档的 git 历史）。
6. 收尾：跑一遍 `PROJECT_INDEX` 链接校验 + 更新 `docs/DOC_AUDIT`。

---

## 5. 预期结果

- 接手“必读核心面”：散在十几处 → **~30 份集中**。
- 仓库项目文档：**151 → ~90**（归档的仍在 `07_archive` 可查）。
- node_modules 64 份从“文件搜索噪声”里消失。
- 账实一致：一张编号映射总表说了算。

---

## 6. 回滚

全程 git；归档 = 有记录的移动；不物理删任何“事实类”文档。任何一步可 `git revert`。

---

## 7. 待用户拍板的 3 个决策点

1. `CHATGPT_项目全景手册.md` 是**并入 CLAUDE** 还是**留作对外简介**？
2. 历史目录**只建映射表不改名**（推荐）还是**真重命名统一**（断链风险）？
3. `tools/frida_build/node_modules` 本地目录**直接删**（需要时 npm 重装）还是**留着**？

---

## 8. 档 B 执行记录（2026-06-10，已执行）

> 触发：用户「HOOKMAP 基本完成 → 归档」。仅执行**档 B（归档）**；档 A 的 P17 重复目录合并一并完成；档 C/E（合并/编号统一）本轮**未动**。

- **git 快照**：归档前先提交在途脏文件作为基线（commit `5bc4c4ce`）。
- **P17 重复目录**：繁体 `P17_会話LSPosed` 并入简体 `P17_会话LSPosed` 后删除（无唯一内容丢失）。
- **归档 21 个任务**（`03_execute_执行任务/` → `07_archive_归档/`）：P15 / P16 / P17 / P19 / P19B / P20 / P21 / P23 / P25 / P26 / P1A / P1B / P1C / P1E / P1F / P22_SearchCrawler / T09 / E2 / P_CV1 / P_ConvWarm / P_IMPORT。清单见 `07_archive_归档/INDEX.md`。
- **保留在 03_execute 的 8 个**（进行中/待办/v1.1/未来）：M6a / P18 / P20B / P20C / P22_PushFilter / P26B / P26C / V3T5。
- **链接**：TASK_BOARD / HOOKMAP / `docs/HOOK_MAP_8071_AUTHORITATIVE` / PROTECTION_MAP / AUTHORIZATIONS / dynamic_crawler README / guard-security skill 中指向已归档任务的路径已改 `07_archive_归档/`。`04_review/` 的 2026-05-27 / P16 等**历史快照**与 `docs/archive/wechat_8066/` 卷宗按惯例**不改**。
- **回退**：`git revert <归档提交>` 整体回退；或 `git mv` 单任务搬回。
- **未做（留给后续档 C/E）**：CHATGPT 全景手册合并、node_modules 清理、编号统一映射表、`.cursor`↔`.claude` skill 镜像同步（guard-security 已改 `.cursor` 一侧，`.claude` 镜像待 `sync_skills.ps1` 统一）。
