# P16 朋友圈过滤 — 审稿交接快照 2026-05-20

## 今日完成

- ✅ D1：密友帖子整条隐藏（`ArrayList.addAll` → `na4.b.d.c1().field_userName`）
- ✅ D2/D3：密友点赞/评论隐藏（`LinkedList.add` → `z15.e56/cs5.di0.d`=wxid → block）
- ✅ HOOKMAP 更新（L0v4 新增）
- ✅ 死路归档（TimeLineObject/getCommentList/LikeUserList 全部证伪）

## 复核要点

### 证据链完整性

| 结论 | 证据 | 等级 |
|------|------|------|
| D1 帖子隐藏生效 | 截图：密友帖子不显示 | 装机实证 ✅ |
| D2/D3 赞评阻断 | logcat: `[MF] LLadd blocked: cs5.di0` | 装机实证 ✅ |
| `getCommentList()` size=0 | logcat: `[D2D3:cmList] size=0` | 装机实证 ✅ |
| TimeLineObject 无 List getter | Frida 堆扫描输出 0 个关键词 | Frida 实证 ✅ |

### 需复核的风险点

1. **`LinkedList.add()` 全局 hook 频率** — 是否影响微信非朋友圈功能（消息列表/会话等）？
   - 当前代码：跳过 `java.*` / `android.*` / `androidx.*` / `kotlin.*` 包的元素
   - 微信内部类（如 `cs5.di0`）才进入 wxid 比对
   - **需确认**：是否有其他微信内部类被误伤（元素 `.d` 字段非 wxid 但值恰好在 hidden 集合中）

2. **`sDiagSeen` 静态字段跨安装残留** — 重装后 key 不清零，诊断日志只跑一次可能跳过
   - 低风险：诊断代码不影响功能逻辑

3. **e2 Adapter 类在 8.0.71 不存在** — `getItemCount` 防跳顶 hook 未挂上
   - 影响：快速下滑偶发跳顶（UX 问题，非功能问题）
   - 待做：找 8.0.71 真实 adapter 类名

4. **KPI 未跑** — `frida_stats.js` 还没执行，红线未确认

## 给复核员的操作清单

- [ ] 确认 `LLadd blocked` 日志只在朋友圈滑动时出现，不在会话列表出现
- [ ] 确认密友帖子上的点赞行（♡ 熵）和评论行（熵: xxx）消失
- [ ] 自己发帖子，非密友评论仍然可见
- [ ] 跑 `frida_stats.js`，贴 KPI 数字

## 未完成

- [ ] `frida_stats.js` KPI 数据
- [ ] 8.0.71 adapter 类名（防跳顶，低优先级）
- [ ] FAILURE_LOG 新增条目（由总调度写）：getCommentList 懒加载、TimeLineObject 无 List getter
